# 待你过目 / 需要拍板的事项

> 这份文件是给**你**看的：我替你做了哪些决定、哪些地方有风险、以及你想改的话改哪里。
> 你说过"需要审批的按默认推荐来"，所以下面每条都已经按 **推荐做法** 实施了 —— 你只需要在
> 早上扫一眼，觉得哪条不对就说一声，我改回来。
>
> 更新时间：2026-09-20（本次：主区重新分工，未提交前先说清楚）
>
> **本次改动（按你昨晚的三条要求做的）**：
>
> 1. **"工具区有点太占用空间" + "工具区可以放到底部，'开始、取消、继续修复'这里作成一个按钮，
>    点开工具区就出现弹窗可以设置"** —— 工具区整体搬进「控制台」弹窗（底栏一个「控制台」按钮），
>    主区三栏拿到全部高度；运行操作（开始/取消/继续修复/改后重跑/打开运行目录）在弹窗的「运行」页。
> 2. **"模型对话放到右边"** —— 右列成了页签：`模型对话` / `校验与输出`，对话默认在前。
> 3. **"对话也可以选中代码进行对话，询问代码"** —— 在中栏选中代码（或 diff、或对话记录）右键，
>    菜单第一项就是「就选中的代码提问」，引用会带进对话并在输入框上方显示，可取消；
>    引用原样出现在你发出的消息里（不是隐藏注入）。
> 4. **"增加修改后是否接受"** —— 提议栏现在是三个按钮：**接受 / 接受并重跑 / 拒绝**。
>    「接受并重跑」= 接受 + 改后重跑（shellcheck → 人工确认 → 执行），闸门一个都没少。
>
> 需要你在**你那边**确认的两件事写在本节末尾（Windows 上的观感我这边看不到）。
>
> **最新结果（今天，第 1、2 步已落地）**：`703025f` —— 工具区默认收起（318px → 40px，高度还给脚本视图）、
> 模型给出脚本时中栏直接出**红绿 diff** 并询问**接受 / 拒绝**（接受只放进中栏、不执行）。
> 后续计划：第 3 步流式实时更新 diff（现已做到 400ms 节流）、第 4 步把设置/自检/历史收进对话框。
>
> **凌晨第二次追加**：`c25fc77` 修掉"Windows 上检测不到 codeagent / 模型无法选 /
> 组件路径歧义"三条。其中**检测不到是真 bug**：Windows 上 npm 装的 CLI 是 `.cmd` 包壳，
> `shutil.which` 找得到但 `CreateProcess` 不能执行 `.cmd` → 现在经 `cmd.exe /c` 起。
> 详见第六节。
>
> **上午结果**：`a59bbdd` 修掉高 DPI 下布局被压爆的三条症状（左栏出框、对话面板与
> 输入框重叠、右栏被压缩）——根因是窗口最小尺寸写死 960x780（Windows 150% 时 1080p 只有 720
> 逻辑像素高）、竖向最小高度堆太高、以及 QSS padding 不计入标签 sizeHint。详见下面第五节。
>
> **前一轮结果**：`4bfb186` 已推送到 GitHub —— 可切换后端 agent（opencode / claude / codeagent / 自定义命令行）。
> 提交前实测：新测试 72 passed、受影响测试组 83 passed（合计提交前一次跑 129 passed）、
> 设置里换后端确实换掉引擎用的适配器（opencode→OpencodeAdapter，codeagent/claude/custom→CliAgentAdapter）、
> 应用 `--self-test` exit=0、`ui/**` 分层规矩未破。详见下面第一节第 1、2 条。

---

## 一、我替你拍板的（已实施，可回退）

| # | 事项 | 我采用的默认（推荐） | 为什么这么定 | 你想改的话 |
| --- | --- | --- | --- | --- |
| 1 | 多后端 agent 的**默认后端** | 仍是 `opencode` | 你现有的凭据、模型、会话都在 opencode 上；换默认会让已有运行记录找不到会话 | 设置 → 后端 agent 下拉里换，或告诉我改成默认 `codeagent` |
| 2 | `codeagent` 的 flags | 按 `claude` 同源实现（`-p`、`--output-format stream-json`、`--session-id`/`--resume`、`--disallowedTools`） | 你说过"flags 应该与 claude 一致" | 点「检测」看结果；不符就把 `codeagent --help` 给我，我按实际 flags 调 |
| 3 | 命令行后端的**结构化输出** | 改成文本契约（`===TU-SCRIPT===` / `===TU-NOTES===` / `===TU-ASSUMPTIONS===` / `===TU-END===` 四段标记），解析成原来的 `GeneratedScript` | 命令行 agent 没有 JSON-schema 能力；这是唯一一条真正改变行为的差异 | 有更好的格式约定就提，解析器在一个文件里，改起来集中 |
| 4 | 命令行后端的**权限** | 默认用 `--disallowedTools` 禁掉执行类工具，`--permission-mode` 收紧 | 保持"agent 只写、引擎唯一执行"这条地基；换后端不放宽安全模型 | 不建议改；确有需要再加白名单 |
| 5 | 半透明 / 亚克力模糊 | 两种模式都改成**界面自绘底色**，窗口不依赖系统透明 | 你在 Windows 11 实测两个都不生效（系统限制：透明属性要无边框窗口、DWM 亚克力要不要重定向位图）；自绘两端一致 | 想要"真正的窗口透明"只能做成无边框自绘标题栏，工作量中等，你说一声 |
| 6 | 「系统提供」与「界面自绘」两个选项 | 合并成一个「亚克力模糊」，来源自动（现在就是自绘） | 你自己的反馈："两个没有区别了" —— 那是冗余 | 想拆回去也行，但结论仍是同一个实现 |
| 7 | 模板库位置 | 移进仓库 `templates/`，源码检出默认用它；打包产物退回用户数据目录 | 你要的"模板随代码版本化"；CWD 相对路径在 exe 上会忽有忽无 | 想放别处就改 `default_templates_dir()` |
| 8 | 生成脚本用的模型 | `deepseek/deepseek-v4-pro`（你已认证的 provider） | 你的 opencode 没有默认模型时会落到免费档，而免费档只允许官方客户端调用 —— 就是你那次"对话失败"的原因 | 设置 → 脚本生成 → 模型里换 |
| 9 | 界面文案口径 | 一律书面语，去掉口语与括号旁白（"生成脚本用"、"问系统要"、"挑一个"等已清除） | 你反复强调过两次 | 有具体措辞不喜欢，直接给我替换后的句子 |

## 一点五、实测补充（夜里验证到的，对你早上有用）

- 本机的 `claude`（2.1.112）**已经登录可用**：我用它跑了一次真实调用，返回正常、退出码 0，
  默认模型就是你配的 `deepseek-v4-pro`。所以新功能落地后，你可以先在 Linux 上直接拿 `claude`
  当后端试（`codeagent` 的替身），不必等 Windows。
- 我顺带验证了权限 flag **真的生效**：加 `--disallowedTools "Bash"` 之后，CLI 自报的工具列表里
  已经没有 `Bash` —— 也就是"agent 只写、引擎唯一执行"这条在命令行后端上同样成立，不是纸面约定。
- 事件形状也实测过了（`system/init` → `assistant`（可能只有 thinking 块）→ `assistant`（正文）→ `result`），
  已把这份证据交给实现方校准解析器；`thinking` 块要跳过、同一条消息的多个事件不能重复追加正文。

## 二、需要你在**你那边**做的事（我做不了）

| # | 事项 | 怎么做 | 备注 |
| --- | --- | --- | --- |
| A | Windows 打包脚本重跑 | `packaging\windows\build.bat` | 已修掉两处：脚本改成纯 ASCII（cmd 读 UTF-8 批处理会把中文切断、碎片被当命令执行）、插件检查改成递归搜索（原先写死的路径在 Windows 上不存在） |
| B | Windows 上确认两个背景效果 | 设置 → 外观 → 背景效果，切「半透明」与「亚克力模糊」各看一眼 | 现在两端都是自绘底色：半透明=壁纸清晰+淡色，亚克力=壁纸模糊+深色。若仍是纯色，状态栏会提示"未找到壁纸图片" |
| C | Windows 的 DWM 真模糊 | 不做也行 | 自绘已经能出毛玻璃效果；真·系统模糊要求窗口没有重定向位图（Qt 普通窗口做不到），要做得改成无边框窗口 |
| D | GitHub Release（可选） | 需要一台 Windows 打出 exe | 仓库里只存源码（`dist/`、`build/` 按约定不入库） |

## 三、我知道但**没有**动的（等你决定）

- **仓库里的开发过程文件**：`.superpowers/`（任务 brief、评审记录）一直是本地文件、没有入库。
  要留档可以入库，要清理可以删 —— 现在保持原样。
- **旧的本地模板目录** `.tu-templates/`：已不再使用（改用仓库 `templates/`），我没删。
  确认没问题后你可以自己删，或让我删。
- **`glms-3` 那个 provider**：`baseURL` 是 Anthropic 兼容端点却挂了 openai-compatible 的 SDK，
  选它多半报错；还有个空的 provider `"12"`。这两个都在你的 `~/.config/opencode/opencode.json` 里，
  我没动（那是别家工具的配置）。
- **无界面 CLI 的默认运行根**：仍是 `./.tu-runs`（跟随当前目录）。要不要跟界面一样走用户数据目录，你定。

## 五、高 DPI 布局（2026-09-20 凌晨，用户报 Windows 上三条症状）

| 症状 | 根因 | 修法与验证 |
| --- | --- | --- |
| 左栏文字出框 | `QLabel.sizeHint()` 不含 QSS 的 padding，QFormLayout 按 sizeHint 分列 → 中文标签每种缩放都窄 4px | 新增 `theme.fit_form_labels()`，按当前字体实测宽度撑开标签列，应用外观后自动重算；三个缩放档位断言不出框 |
| 对话面板与输入框重叠 | 记录 120 + 输入 72 把面板顶到 324，窗口地板被抬到 666 | 记录 64、输入 56 → 面板 252；四种小尺寸下断言不重叠（变异验证转红） |
| 右栏被压缩 | 三段最小高度 110+110+80=488，720 高的屏幕上主区只剩约 412 | 降到 52/52/40（合计 332），仍保留"看得见几行"的下限 |

**共同的最大根因**：窗口最小尺寸写死 `960x780`。Windows 150% 缩放下 1080p 只有 **720** 逻辑像素高
—— 最小尺寸比屏幕还高，窗口管理器照给，布局只能违反最小尺寸。现在改成按**屏幕可用区**收敛
（`window_minimum_for()`：九成宽 / 八成半高，并保留 720x520 下限），并在用例里钉住"最小尺寸不得
超过屏幕"。

**我没法在 Windows 上验证**，所以用的是"本地复现 + 机制断言"：在你那种缩放条件下（约 700 逻辑
像素高）本地已复现出完全相同的三条症状，修完在 1440x900 / 1280x720 / 1100x640 / 960x600 / 900x560
五档尺寸下都不再重叠或出框。请你在 Windows 上再确认一次观感。

## 六、Windows 后端 agent 三条问题（2026-09-20 凌晨，用户报）

| 你报的现象 | 根因 | 修法 |
| --- | --- | --- |
| **后端有 codeagent，但检测不到** | Windows 上 npm/包管理器装的 CLI 是 `codeagent.cmd`（+`.ps1`）包壳：`shutil.which` 找得到，但 `subprocess` 走 CreateProcess，**不执行 `.cmd`/`.bat`** → `[WinError 193]` | 新增 `agent_backends/invocation.py`：`.cmd`/`.bat` 经 `cmd.exe /c`、`.ps1` 经 `powershell -File`；**探测与真正跑任务共用同一个转换**（否则会出现"检测通过、一跑就失败"更难的错）。7 条用例注入平台把这套形状钉住 |
| **模型无法选择和检测可用模型** | 命令行后端没有"列出模型"的命令，按钮只说一句"本后端不提供" | 后端文件里声明 `model_suggestions` / `model_hint`：候选填进可编辑下拉（claude 系：sonnet / opus / haiku），点检测给出候选 + "取值原样传给 `--model`、可手输、留空用后端默认"的说明；opencode 仍走运行时列出 |
| **组件路径还是 opencode，选了 codeagent 怎么办** | 那一行只在后端是 opencode 时有意义，界面没说明 | 非 opencode 后端时该行**置灰 + 改标签**「opencode（仅该后端使用）」，并新增说明：agent 命令在「后端 agent」里设置；**Git Bash 与 shellcheck 任何后端都需要**（引擎执行脚本用的是它们，换 agent 不换执行者）|

**验证**：新增用例 7 条，两条变异验证都转红（撤掉 `.cmd` 包裹 → 检测用例红；撤掉置灰 → 路径用例红）；
相关测试组 153 passed。**这些仍需你在 Windows 上确认** —— 尤其第一条：重跑后点「检测」，
若能显示出 `codeagent x.y.z` 版本号，就说明包壳问题解决了。

## 七、主区重新分工：对话上右栏 / 工具区进「控制台」（2026-09-20，按你昨晚三条要求）

### 我替你拍板的

| # | 事项 | 采用的默认 | 为什么 | 你想改的话 |
| --- | --- | --- | --- | --- |
| 1 | 「控制台」按钮的**名字** | 底栏按钮叫「控制台」，提示写着"运行操作、历史运行、模板库、环境自检、设置" | 它现在装的是五件低频事（含运行操作），叫"工具区"会把"开始/取消"这类主操作藏得太深；叫"控制台"最直白 | 想叫「工具」「更多」都行，改一个字符串 |
| 2 | 运行操作为什么也进弹窗 | 「开始 / 取消 / 继续修复 / 改后重跑 / 打开运行目录」全在弹窗「运行」页 | 你说的是"开始、取消、继续修复这里作成一个按钮，点开就出现弹窗" —— 这一整块都进去了。**代价**：开始一次运行要多点一下（底栏「控制台」→「开始」）。若嫌麻烦我可以把「开始」单独留在底栏、其余进弹窗 | 说一声就改 |
| 3 | 右列页签顺序 | `模型对话` 在前、`校验与输出` 在后，**默认显示对话** | 你说"模型对话放到右边"；对话是运行期间一直要看的那页，校验报告是事后核对 | 想默认显示校验报告就说 |
| 4 | 引用的**上限** | 单次引用最多 120 行 / 6000 字符，超出截断并在消息里写明"引用已截断" | 选中整份脚本（几百行）一次发给模型既贵又慢，模型注意力也会散；**不悄悄少发**，你发出去的消息里能看到截断说明 | 想放大到"整份都发"就改 `chat.py` 里那两个常量 |
| 5 | 引用**不进历史注入** | 引用原样拼在消息里，你在对话记录里能看到自己发了什么 | 隐藏注入提示词会让"我到底问了什么"变得不可查 —— 这个软件里每一步都要能对上账 | 不建议改 |
| 6 | 提议栏三个按钮 | **接受 / 接受并重跑 / 拒绝** | 「接受并重跑」= 接受 + 改后重跑（shellcheck → 人工确认 → 执行）。快捷的是点击次数，**不是安全边界**：执行前那道人工确认照旧弹 | 不想要第三个按钮就说，删掉即可 |
| 7 | 主窗口那条竖向分割器 | **删除**（"三栏区 / 历史与设置区"的比例） | 低频面板搬走后它没东西可分了。设置里遗留的 `vertical` 键会被忽略，不会弄坏你的旧布局 | 想留一条空的分割条没意义；不建议 |
| 8 | 右栏栏头 | 「校验与输出」栏头**删除**，标题由页签承担 | 页签上已经写着「校验与输出」，再来一行标题是重复占高度 | 想恢复栏头也能加回来 |

### 需要你在 Windows 上确认的（我这边看不到）

1. **弹窗在 150% 缩放下放不下**：弹窗固定 860×560，1080p + 150% 时有约 720 逻辑像素高，
   理论够用；若你的屏幕上它贴边或被裁，告我一声，我按可用高度收敛（与主窗口同一套算法）。
2. **右列页签在你的字体下会不会挤**：页签文字是中文，Windows 字体度量与我这里不同，
   若「模型对话 / 校验与输出」两个页签被截断，告我一声。

### 这次改动的验证

- 整套用例 **456 passed / 1 skipped**；应用 `--self-test` exit=0（"外观自检通过"）。
- 新增 `tests/test_chat_selection.py` 11 条（选中范围与来源、换行符、菜单项、引用进消息、
  发完即撤、截断、取消引用、不挤输入框、记录区引用、整窗两条链路）；
  `tests/test_script_proposal.py` 加 2 条；`tests/test_theme.py` 加 1 条。
- 变异验证：撤掉 `install_ask_action` 的接线 → 两条链路用例转红；撤掉 `_clamp_quote` → 截断用例转红；
  把 `_apply_pending_proposal` 改成直接返回 True → 空操作用例转红。

## 四、如果早上你看到有东西不对

按这个顺序找证据，比"感觉不对"有用：

1. 界面底部状态栏 / 自检页的一行结论（哪里失败会写在那里）；
2. 运行目录里的 `meta.json` 与 `server.log`（opencode 路径）或命令行后端的原始输出；
3. `git log --oneline -10` 看我这晚做了什么，每条提交信息里都写了"为什么"；
4. 直接告我现象，我按"像素/日志/复现"三步定位 —— 这一晚的每个修复都是这么做的。
