# 待你过目 / 需要拍板的事项

> 这份文件是给**你**看的：我替你做了哪些决定、哪些地方有风险、以及你想改的话改哪里。
> 你说过"需要审批的按默认推荐来"，所以下面每条都已经按 **推荐做法** 实施了 —— 你只需要在
> 早上扫一眼，觉得哪条不对就说一声，我改回来。
>
> 更新时间：2026-09-20（本次：主区重新分工，未提交前先说清楚）
>
> **本次改动（按你昨晚的三条要求做的）**：
>
> 1. **"工具区有点太占用空间" + "工具区可以放到底部，'开始、取消、继续修复'这里作成一个按钮，
>    点开工具区就出现弹窗可以设置"** —— 工具区整体搬进「控制台」弹窗（底栏一个「控制台」按钮），
>    主区三栏拿到全部高度；运行操作（开始/取消/继续修复/改后重跑/打开运行目录）在弹窗的「运行」页。
> 2. **"模型对话放到右边"** —— 右列成了页签：`模型对话` / `校验与输出`，对话默认在前。
> 3. **"对话也可以选中代码进行对话，询问代码"** —— 在中栏选中代码（或 diff、或对话记录）右键，
>    菜单第一项就是「就选中的代码提问」，引用会带进对话并在输入框上方显示，可取消；
>    引用原样出现在你发出的消息里（不是隐藏注入）。
> 4. **"增加修改后是否接受"** —— 提议栏现在是三个按钮：**接受 / 接受并重跑 / 拒绝**。
>    「接受并重跑」= 接受 + 改后重跑（shellcheck → 人工确认 → 执行），闸门一个都没少。
>
> 需要你在**你那边**确认的两件事写在本节末尾（Windows 上的观感我这边看不到）。
>
> **最新结果（今天，第 1、2 步已落地）**：`703025f` —— 工具区默认收起（318px → 40px，高度还给脚本视图）、
> 模型给出脚本时中栏直接出**红绿 diff** 并询问**接受 / 拒绝**（接受只放进中栏、不执行）。
> 后续计划：第 3 步流式实时更新 diff（现已做到 400ms 节流）、第 4 步把设置/自检/历史收进对话框。
>
> **凌晨第二次追加**：`c25fc77` 修掉"Windows 上检测不到 codeagent / 模型无法选 /
> 组件路径歧义"三条。其中**检测不到是真 bug**：Windows 上 npm 装的 CLI 是 `.cmd` 包壳，
> `shutil.which` 找得到但 `CreateProcess` 不能执行 `.cmd` → 现在经 `cmd.exe /c` 起。
> 详见第六节。
>
> **上午结果**：`a59bbdd` 修掉高 DPI 下布局被压爆的三条症状（左栏出框、对话面板与
> 输入框重叠、右栏被压缩）——根因是窗口最小尺寸写死 960x780（Windows 150% 时 1080p 只有 720
> 逻辑像素高）、竖向最小高度堆太高、以及 QSS padding 不计入标签 sizeHint。详见下面第五节。
>
> **前一轮结果**：`4bfb186` 已推送到 GitHub —— 可切换后端 agent（opencode / claude / codeagent / 自定义命令行）。
> 提交前实测：新测试 72 passed、受影响测试组 83 passed（合计提交前一次跑 129 passed）、
> 设置里换后端确实换掉引擎用的适配器（opencode→OpencodeAdapter，codeagent/claude/custom→CliAgentAdapter）、
> 应用 `--self-test` exit=0、`ui/**` 分层规矩未破。详见下面第一节第 1、2 条。

---

## 一、我替你拍板的（已实施，可回退）

| # | 事项 | 我采用的默认（推荐） | 为什么这么定 | 你想改的话 |
| --- | --- | --- | --- | --- |
| 1 | 多后端 agent 的**默认后端** | 仍是 `opencode` | 你现有的凭据、模型、会话都在 opencode 上；换默认会让已有运行记录找不到会话 | 设置 → 后端 agent 下拉里换，或告诉我改成默认 `codeagent` |
| 2 | `codeagent` 的 flags | 按 `claude` 同源实现（`-p`、`--output-format stream-json`、`--session-id`/`--resume`、`--disallowedTools`） | 你说过"flags 应该与 claude 一致" | 点「检测」看结果；不符就把 `codeagent --help` 给我，我按实际 flags 调 |
| 3 | 命令行后端的**结构化输出** | 改成文本契约（`===TU-SCRIPT===` / `===TU-NOTES===` / `===TU-ASSUMPTIONS===` / `===TU-END===` 四段标记），解析成原来的 `GeneratedScript` | 命令行 agent 没有 JSON-schema 能力；这是唯一一条真正改变行为的差异 | 有更好的格式约定就提，解析器在一个文件里，改起来集中 |
| 4 | 命令行后端的**权限** | 默认用 `--disallowedTools` 禁掉执行类工具，`--permission-mode` 收紧 | 保持"agent 只写、引擎唯一执行"这条地基；换后端不放宽安全模型 | 不建议改；确有需要再加白名单 |
| 5 | 半透明 / 亚克力模糊 | 两种模式都改成**界面自绘底色**，窗口不依赖系统透明 | 你在 Windows 11 实测两个都不生效（系统限制：透明属性要无边框窗口、DWM 亚克力要不要重定向位图）；自绘两端一致 | 想要"真正的窗口透明"只能做成无边框自绘标题栏，工作量中等，你说一声 |
| 6 | 「系统提供」与「界面自绘」两个选项 | 合并成一个「亚克力模糊」，来源自动（现在就是自绘） | 你自己的反馈："两个没有区别了" —— 那是冗余 | 想拆回去也行，但结论仍是同一个实现 |
| 7 | 模板库位置 | 移进仓库 `templates/`，源码检出默认用它；打包产物退回用户数据目录 | 你要的"模板随代码版本化"；CWD 相对路径在 exe 上会忽有忽无 | 想放别处就改 `default_templates_dir()` |
| 8 | 生成脚本用的模型 | `deepseek/deepseek-v4-pro`（你已认证的 provider） | 你的 opencode 没有默认模型时会落到免费档，而免费档只允许官方客户端调用 —— 就是你那次"对话失败"的原因 | 设置 → 脚本生成 → 模型里换 |
| 9 | 界面文案口径 | 一律书面语，去掉口语与括号旁白（"生成脚本用"、"问系统要"、"挑一个"等已清除） | 你反复强调过两次 | 有具体措辞不喜欢，直接给我替换后的句子 |

## 一点五、实测补充（夜里验证到的，对你早上有用）

- 本机的 `claude`（2.1.112）**已经登录可用**：我用它跑了一次真实调用，返回正常、退出码 0，
  默认模型就是你配的 `deepseek-v4-pro`。所以新功能落地后，你可以先在 Linux 上直接拿 `claude`
  当后端试（`codeagent` 的替身），不必等 Windows。
- 我顺带验证了权限 flag **真的生效**：加 `--disallowedTools "Bash"` 之后，CLI 自报的工具列表里
  已经没有 `Bash` —— 也就是"agent 只写、引擎唯一执行"这条在命令行后端上同样成立，不是纸面约定。
- 事件形状也实测过了（`system/init` → `assistant`（可能只有 thinking 块）→ `assistant`（正文）→ `result`），
  已把这份证据交给实现方校准解析器；`thinking` 块要跳过、同一条消息的多个事件不能重复追加正文。

## 二、需要你在**你那边**做的事（我做不了）

| # | 事项 | 怎么做 | 备注 |
| --- | --- | --- | --- |
| A | Windows 打包脚本重跑 | `packaging\windows\build.bat` | 已修掉两处：脚本改成纯 ASCII（cmd 读 UTF-8 批处理会把中文切断、碎片被当命令执行）、插件检查改成递归搜索（原先写死的路径在 Windows 上不存在） |
| B | Windows 上确认两个背景效果 | 设置 → 外观 → 背景效果，切「半透明」与「亚克力模糊」各看一眼 | 现在两端都是自绘底色：半透明=壁纸清晰+淡色，亚克力=壁纸模糊+深色。若仍是纯色，状态栏会提示"未找到壁纸图片" |
| C | Windows 的 DWM 真模糊 | 不做也行 | 自绘已经能出毛玻璃效果；真·系统模糊要求窗口没有重定向位图（Qt 普通窗口做不到），要做得改成无边框窗口 |
| D | GitHub Release（可选） | 需要一台 Windows 打出 exe | 仓库里只存源码（`dist/`、`build/` 按约定不入库） |

## 三、我知道但**没有**动的（等你决定）

- **仓库里的开发过程文件**：`.superpowers/`（任务 brief、评审记录）一直是本地文件、没有入库。
  要留档可以入库，要清理可以删 —— 现在保持原样。
- **旧的本地模板目录** `.tu-templates/`：已不再使用（改用仓库 `templates/`），我没删。
  确认没问题后你可以自己删，或让我删。
- **`glms-3` 那个 provider**：`baseURL` 是 Anthropic 兼容端点却挂了 openai-compatible 的 SDK，
  选它多半报错；还有个空的 provider `"12"`。这两个都在你的 `~/.config/opencode/opencode.json` 里，
  我没动（那是别家工具的配置）。
- **无界面 CLI 的默认运行根**：仍是 `./.tu-runs`（跟随当前目录）。要不要跟界面一样走用户数据目录，你定。

## 五、高 DPI 布局（2026-09-20 凌晨，用户报 Windows 上三条症状）

| 症状 | 根因 | 修法与验证 |
| --- | --- | --- |
| 左栏文字出框 | `QLabel.sizeHint()` 不含 QSS 的 padding，QFormLayout 按 sizeHint 分列 → 中文标签每种缩放都窄 4px | 新增 `theme.fit_form_labels()`，按当前字体实测宽度撑开标签列，应用外观后自动重算；三个缩放档位断言不出框 |
| 对话面板与输入框重叠 | 记录 120 + 输入 72 把面板顶到 324，窗口地板被抬到 666 | 记录 64、输入 56 → 面板 252；四种小尺寸下断言不重叠（变异验证转红） |
| 右栏被压缩 | 三段最小高度 110+110+80=488，720 高的屏幕上主区只剩约 412 | 降到 52/52/40（合计 332），仍保留"看得见几行"的下限 |

**共同的最大根因**：窗口最小尺寸写死 `960x780`。Windows 150% 缩放下 1080p 只有 **720** 逻辑像素高
—— 最小尺寸比屏幕还高，窗口管理器照给，布局只能违反最小尺寸。现在改成按**屏幕可用区**收敛
（`window_minimum_for()`：九成宽 / 八成半高，并保留 720x520 下限），并在用例里钉住"最小尺寸不得
超过屏幕"。

**我没法在 Windows 上验证**，所以用的是"本地复现 + 机制断言"：在你那种缩放条件下（约 700 逻辑
像素高）本地已复现出完全相同的三条症状，修完在 1440x900 / 1280x720 / 1100x640 / 960x600 / 900x560
五档尺寸下都不再重叠或出框。请你在 Windows 上再确认一次观感。

## 六、Windows 后端 agent 三条问题（2026-09-20 凌晨，用户报）

| 你报的现象 | 根因 | 修法 |
| --- | --- | --- |
| **后端有 codeagent，但检测不到** | Windows 上 npm/包管理器装的 CLI 是 `codeagent.cmd`（+`.ps1`）包壳：`shutil.which` 找得到，但 `subprocess` 走 CreateProcess，**不执行 `.cmd`/`.bat`** → `[WinError 193]` | 新增 `agent_backends/invocation.py`：`.cmd`/`.bat` 经 `cmd.exe /c`、`.ps1` 经 `powershell -File`；**探测与真正跑任务共用同一个转换**（否则会出现"检测通过、一跑就失败"更难的错）。7 条用例注入平台把这套形状钉住 |
| **模型无法选择和检测可用模型** | 命令行后端没有"列出模型"的命令，按钮只说一句"本后端不提供" | 后端文件里声明 `model_suggestions` / `model_hint`：候选填进可编辑下拉（claude 系：sonnet / opus / haiku），点检测给出候选 + "取值原样传给 `--model`、可手输、留空用后端默认"的说明；opencode 仍走运行时列出 |
| **组件路径还是 opencode，选了 codeagent 怎么办** | 那一行只在后端是 opencode 时有意义，界面没说明 | 非 opencode 后端时该行**置灰 + 改标签**「opencode（仅该后端使用）」，并新增说明：agent 命令在「后端 agent」里设置；**Git Bash 与 shellcheck 任何后端都需要**（引擎执行脚本用的是它们，换 agent 不换执行者）|

**验证**：新增用例 7 条，两条变异验证都转红（撤掉 `.cmd` 包裹 → 检测用例红；撤掉置灰 → 路径用例红）；
相关测试组 153 passed。**这些仍需你在 Windows 上确认** —— 尤其第一条：重跑后点「检测」，
若能显示出 `codeagent x.y.z` 版本号，就说明包壳问题解决了。

## 七、主区重新分工：对话上右栏 / 工具区进「控制台」（2026-09-20，按你昨晚三条要求）

### 我替你拍板的

| # | 事项 | 采用的默认 | 为什么 | 你想改的话 |
| --- | --- | --- | --- | --- |
| 1 | 「控制台」按钮的**名字** | 底栏按钮叫「控制台」，提示写着"运行操作、历史运行、模板库、环境自检、设置" | 它现在装的是五件低频事（含运行操作），叫"工具区"会把"开始/取消"这类主操作藏得太深；叫"控制台"最直白 | 想叫「工具」「更多」都行，改一个字符串 |
| 2 | 运行操作为什么也进弹窗 | 「开始 / 取消 / 继续修复 / 改后重跑 / 打开运行目录」全在弹窗「运行」页 | 你说的是"开始、取消、继续修复这里作成一个按钮，点开就出现弹窗" —— 这一整块都进去了。**代价**：开始一次运行要多点一下（底栏「控制台」→「开始」）。若嫌麻烦我可以把「开始」单独留在底栏、其余进弹窗 | 说一声就改 |
| 3 | 右列页签顺序 | `模型对话` 在前、`校验与输出` 在后，**默认显示对话** | 你说"模型对话放到右边"；对话是运行期间一直要看的那页，校验报告是事后核对 | 想默认显示校验报告就说 |
| 4 | 引用的**上限** | 单次引用最多 120 行 / 6000 字符，超出截断并在消息里写明"引用已截断" | 选中整份脚本（几百行）一次发给模型既贵又慢，模型注意力也会散；**不悄悄少发**，你发出去的消息里能看到截断说明 | 想放大到"整份都发"就改 `chat.py` 里那两个常量 |
| 5 | 引用**不进历史注入** | 引用原样拼在消息里，你在对话记录里能看到自己发了什么 | 隐藏注入提示词会让"我到底问了什么"变得不可查 —— 这个软件里每一步都要能对上账 | 不建议改 |
| 6 | 提议栏三个按钮 | **接受 / 接受并重跑 / 拒绝** | 「接受并重跑」= 接受 + 改后重跑（shellcheck → 人工确认 → 执行）。快捷的是点击次数，**不是安全边界**：执行前那道人工确认照旧弹 | 不想要第三个按钮就说，删掉即可 |
| 7 | 主窗口那条竖向分割器 | **删除**（"三栏区 / 历史与设置区"的比例） | 低频面板搬走后它没东西可分了。设置里遗留的 `vertical` 键会被忽略，不会弄坏你的旧布局 | 想留一条空的分割条没意义；不建议 |
| 8 | 右栏栏头 | 「校验与输出」栏头**删除**，标题由页签承担 | 页签上已经写着「校验与输出」，再来一行标题是重复占高度 | 想恢复栏头也能加回来 |

### 过程中发现并修掉的两个尺寸问题（右列变宽之后才暴露出来）

1. **右列的最小宽度把左栏挤扁**：对话面板里两个下拉写死了 240 / 200 的最小宽度，整栏最小宽度
   被顶到 599px —— 1440 宽的窗口里右列吃 599、左栏只剩 284（意图是 320）。改成 150 / 110 之后
   实测三栏回到 **[321, 563, 522]**。
2. **窄栏里底部控件行重叠**：窗口缩到 960 时右列只有 386px，底部那行（发送/取消/把最新脚本
   放进中栏 + 模型下拉 + 可用模型）自然宽度 490px，Qt 的反应是让模型下拉**盖住**「可用模型」
   按钮。新增 `ui/widgets/wrap_row.py`：放不下就**折行**（宽的时候仍是一行、模型选择仍贴右端），
   折行时整组折（「模型」标签跟着它的下拉走）。

### 你睡觉时我又做的一轮：用「150% 缩放」模拟 Windows 形状（2026-09-20 深夜）

你说"确保 windows 版本无误"。开发机是 Linux，真机我碰不到，能做的是**把 Windows 的形状在
本机复现出来**再让用例去撞：`QT_SCALE_FACTOR=1.5`（或 2）跑整套 —— 逻辑屏幕像素按比例缩小
（测试屏 800x800 → 150% 下只有 533x533），所有"按屏幕收敛"的规则都会走进小屏分支。

**撞出一条真 bug（已修）**：窗口最小尺寸可以比屏幕还大。

| 屏幕可用区 | 修前 | 修后 |
| --- | --- | --- |
| 1920x1080 | 960x780 | 960x780（不变） |
| **1280x720（你的 1080p/150%）** | 960x612 | 960x612（不变） |
| 800x800 | 720x680 | 720x680（不变） |
| 533x533（150% 模拟） | **720x520（比屏幕还大）** | 506x506 |
| 640x480 | **720x520（比屏幕还大）** | 608x456 |

"最小尺寸比屏幕还大"正是你报过的那三条症状的根因（窗口管理器照给最小尺寸 → 布局只能违反它）。
**在你的机器上取值完全没变**，变的是更小的屏 —— 顺带把「控制台」弹窗也改成按屏幕收敛
（原来固定 860x560；1366x768 的笔记本在 150% 下只有 512 逻辑像素高，弹窗会比屏幕还高、
底部「关闭」按钮点不到）。弹窗是**装不下才缩**，你自己调过的大小不会被覆盖。

顺手还修了测试方法本身的一个坑：150% 下 `widget.grab()` 是**物理像素**图，
而取色用例都在用逻辑坐标 —— 取到了别的地方（"脚本只读底"取到了行号区的颜色，三条用例假红）。
新增 `tests/conftest.py::Grab` 按 devicePixelRatio 换算。

**Windows 方向核对到的其它结论**（详见 `packaging/build.md` §6.2 与设计文档修订十九）：
- 打包收集：Linux 产物与 Windows 用**同一份 spec**，新模块都在包内自动收集（分析表已确认
  `wrap_row` / `selection_menu` 都在）；模板库由代码内置模板在首次运行时写进用户数据目录，
  打包产物不会出现空模板库；
- CRLF：引用进提示词前统一成 `\n`（Windows 来的文本可能是 CRLF）；
- 两个打包脚本**不需要改**（没有新增数据文件；ASCII/CRLF、共用 spec、抽查同一组 Qt 插件
  这几条都有契约用例守着）；
- `packaging/build.md` 新增 §6.2 手测清单 B1~B9（控制台弹窗 / 右列页签 / 选中引用 /
  接受并重跑 / 窄栏折行 / 150% 缩放），你在 Windows 上照着勾就行。

**顺手清掉的一类 Windows 风险**：`ui/backdrop.py` 里还留着 DWM/KWin 那两条"问系统要模糊"的
老路（`try_enable_blur` / `_enable_windows_acrylic` / `_enable_kde_blur`）和**重复定义两遍**的
`erase_damage` —— 产品里引用数为 0 的死代码：**Windows 专有、开发机跑不到、也没有用例**，
正是"Windows 版本可能出问题"最典型的一类来源（模块说明还停留在旧方案）。已删除，
并把"界面层不许出现平台分支"的豁免名单从两个文件收紧到一个（只剩 Windows 壁纸探测），
变体验证：往别处塞一个 `sys.platform` 分支立刻转红。

**现在的状态**：整套用例在 100% 与 150% 两种缩放下都是 **531 passed / 2 skipped**；
应用 `--self-test` exit=0；Linux 产物重建后自检 exit=0；代码已推送到 GitHub
（`c9d8ef5` → `8969733` → `1b59eb3` → `d26cc43`，`main` 最新）。

### 你截图报的三条（已修，2026-09-20）

| 你报的 | 根因 | 现在 |
| --- | --- | --- |
| **点了控制台没反应** | 真 bug（我的接线错）：`clicked.connect(self.open_console)` 会把 `checked=False` 当成 `page` 传进去 → `indexOf(False)` 抛异常 → 槽函数中断、弹窗永不出现。之前的用例都是**直接调** `open_console`，没有一条走真点击，所以没抓到 | 接线改成用 lambda 吞掉 checked，方法内部也把非控件实参当成"没指定页"；新增"真点一下"的用例 + 一条**通用契约**（所有 `clicked.connect(self.方法)` 的目标都不许有位置参数，带默认值的也算），两条变异全红 |
| **把模型移动到跟发送同一行** | 底栏那一行原来需要 491px，而右列最小宽度只有 385px，所以被折行 | 把这一行的固有宽度压到 330px：`把最新脚本放进中栏`→`存入中栏`（完整含义进提示气泡）、去掉「模型」标签、去掉「可用模型」按钮（并入下拉）。实测三种屏幕、窗口取最小尺寸都是**一行** |
| **可用模型点开用列表呈现** | 原来要先点「可用模型」按钮把候选塞进下拉，再点下拉才看到列表（两步） | 列表并进模型下拉：**点开下拉＝现取现列**，末项是「重新获取可用模型」。另修：弹层原来跟下拉一样宽（130px），`deepseek/deepseek-v4-flash` 被截断 —— 现在按最长模型名撑开；手输模型名仍然可用。取列表加了 300s TTL（opencode 上是子进程，连点下拉不该跑好几次） |

> 注意：**「可用模型」这个按钮现在没有了** —— 它的列表并进了模型下拉（底栏少一个按钮，
> 模型才能跟发送同一行）。如果你更想要那个独立按钮，我把标签改回「可用模型」并加回来，
> 代价是窄窗口下这一行会折成两行。你说一声就行。

### 你截图报的"会话不是圆角"（已修，2026-09-20）

根因量出来了：输入控件的圆角写的是 `radius_lg`(14px)，而这些控件没有 min-height，高度由字体
度量决定 —— Qt 在**半径 >= 高度的一半**时整个退回直角。实测各档：

| 界面缩放 | 控件自然高度 | 半径 | 结果 |
| --- | --- | --- | --- |
| 0.8 | 26px | 11.2px | 圆角（勉强） |
| 1.0 | 29px | 14px | 圆角（只差 0.5px） |
| 1.4 | 35px | 19.6px | **直角** |
| 1.6 | 39px | 22.4px | **直角** |

**你机器上是 1.0 档就已经直角了**：Windows 的字体度量比 Linux 矮，同样的字号下行高更小 ——
所以我本机一直没看到。输入类圆角改成 10px（与按钮一致）之后，各档的"半径/半高"是 0.62~0.84，
都有余量；**所有**单行输入控件（下拉、数字框、文本框）一起修好了。

顺带说明老用例为什么没拦住：它给控件设了 `setMinimumHeight(30)`，正好把半径压进合法区间。
新用例故意不设最小高度，在四档缩放下逐档取角，并且半径是从**真正生效的样式表**里读出来的
（变异验证：改回 `radius_lg` 立刻转红）。

> 关于"黑底"：输入框比面板更深是**故意的**（半透明模式下输入框 alpha 205/255，比内容区更不透明）
> —— 这是之前实测定的：再透一点，下拉里的字体名对比度只有 3.1:1，低于可读线（你报过"看不见字"）。
> 所以这次只改了圆角，底色没动。你要是希望输入框在透明模式下也"透气"，我可以把 alpha 降到
> 160~180 再测一遍对比度，你说一声。

### 输入框"黑底"：按你选的三条都改了（2026-09-20）

先把你那张截图**逐像素量了**：会话框的填充是 rgb(19,21,24) = 输入框底色 `#121317` + 壁纸透出；
那张图里会话框的角**是圆的**（半径约 14px、框高 36px）。两张图哈希完全相同 → 是同一张旧图，
所以"圆角"那条我还需要你重新构建后再看一眼。但"黑底"的观感是真的，按你选的三条一起改了：

| 项 | 改前 | 改后 |
| --- | --- | --- |
| 输入框底色 | `#121317`（接近纯黑，比卡片深） | `#1b1c21`（比卡片 `#17181c` **亮**一档） |
| 输入框描边 | 白 8%（几乎看不见） | 白 **16%**，hover 24% |
| 会话/模型这类**选择框** | 同输入框的黑底 | **按钮那种浅底**（`#232428`，与「扫描历史会话」同类） |

一句话总结思路变化：原来靠"底色比面板更深"来表达形状，深到接近纯黑就成了黑洞；
现在靠"**比卡片亮 + 描边看得见**"表达形状。三处都有像素级用例钉住（阈值是量出来的：
16% 描边与填充的亮度差是 35，8% 只有 16，用例卡在 25），撤掉任何一条都会转红。

> 你重新构建后看一眼：会话框现在应该是**比周围略亮的圆角框**（不是黑块），
> 模型下拉与按钮同色。如果还想更亮/更暗，告诉我方向，改一个令牌就行。

### "会话底下的黑色底色"：找着了，是纯布局容器自己上了色（2026-09-20）

不是你眼花，也不是壁纸：**会话那一行的容器（`#chatSessionRow`）在给自己上色**。它是个纯布局
容器，没有 QSS 规则，于是落进通用 `QWidget { background-color: bg }` 里 —— 半透明/亚克力模式下
整行被盖了一层深色。同一类的还有 `#proposalBar`、底部控件行 `#controlsRow`、右列页签容器
`#rightTabs`（都是右列改版时新加/漏掉的）。

实测（**均匀**壁纸 + 半透明模式）：

| 取样位置 | 修前 | 修后 |
| --- | --- | --- |
| 会话行内的空白 | **46** | 68 |
| 面板内不属于任何容器的空白 | 68 | 68 |

差 22 亮度。这一行大部分被下拉和按钮盖住，**露在外面的恰好就是「会话」标签周围那片** ——
正是你圈的位置。纯色模式看不出来（那层色与面板同色），只有透明模式才暴露。

已修：把 `#rightTabs, #chatSessionRow, #proposalBar, #controlsRow` 加进"任何模式下都不上色"
的纯容器名单；新增用例按"容器内空白 vs 面板空白亮度差 ≤6"钉住，去掉规则立刻转红。
顺手把整窗 + 控制台五个页的**所有**纯容器扫了一遍，没有第二处。

> 方法上的教训也写进文档了：第一版我用你的真实壁纸量，得到"右列比左栏暗 30"的结论、
> 差点把 `#rightTabs` 当成根因 —— 换成均匀壁纸复测，左右一致，问题其实在会话行那一层。
> **截图里的"某块更暗"必须控制变量再判**。

### Windows 上 codeagent 检测不到、也选不了模型（已修，2026-09-20）

你睡前报的这两条都修了。开发机是 Linux，所以做法是**把 Windows 的形状完整模拟出来**再改
（npm 包壳 + PATH 兜底 + 注入平台），这样逻辑在本机就能被用例撞。

**检测**（四条都是"别把装了的东西判成没装"）：

| # | 修前 | 修后 |
| --- | --- | --- |
| 1 | 只试 `--version`，不认就直接失败 | 依次试 `--version` → `-v` → `version` → `--help`；失败提示里写出试过哪些 |
| 2 | 退出码 0 但没打版本号 → 判成没装 | **算找到了**（版本记"未识别"） |
| 3 | 探测没设 stdin，CLI 想读输入就挂到超时（界面上=点了检测没反应） | `stdin=DEVNULL` |
| 4 | 只在 PATH 里找 | PATH 没有就按常见安装目录找：`%APPDATA%\npm`、`%LOCALAPPDATA%\pnpm`、`scoop\shims`、`.cargo\bin`、`.local\bin`、`Program Files\nodejs`（`.exe`→`.cmd`→`.bat`→`.ps1`） |

**参数传递**（本轮顺带发现的真问题）：`.cmd` 只能经 `cmd.exe` 起，而 cmd 会把命令行再解析一遍 ——
裸的 `& | < > ^` 是语法、`%NAME%` 会展开、**换行是命令分隔符**，而生成脚本时
`--append-system-prompt` 的值是**多行**的，经 cmd 传会被切碎。npm 包壳里本来就写着"用哪个 node、
跑哪个 js"，现在直接读出来走 `[node.exe, cli.js, *参数]`，**绕开 cmd**，标点和换行都原样送达；
读不出这种形状才退回 `cmd.exe /d /s /c`（参数逐个做 cmd 转义）。探测与跑任务共用同一个转换。

**选模型**：命令行后端的**对话面板**模型下拉以前只写一句提示就返回，下拉里空空的 ——
现在把后端声明的候选（claude 系：`sonnet` / `opus` / `haiku`）铺进去，仍然可以手输完整模型名。

> 你那边重建后可以这样验证：设置 → 后端 agent 选 codeagent → 点「检测」应显示
> `命令可用：C:\Users\...\npm\codeagent.cmd（版本 x.y.z）`；右列对话的模型下拉点开
> 应该能看到 sonnet / opus / haiku，选中即用于这段对话。若仍失败，把「检测」那一行的提示
> 原文发我 —— 现在它会写明试过哪些参数、失败在第几处。

### 运行按钮回到主界面底栏、控制台挪到最右（2026-09-20，按你的截图改）

| 位置 | 内容 |
| --- | --- |
| 主窗口底栏（一行） | `开始` `取消` `继续修复` `改后重跑` `打开运行目录` → 状态文字 → **最右** `控制台` |
| 控制台弹窗 | `历史运行` `模板库` `环境自检` `设置`（**「运行」页删了** —— 空的页签不如没有） |

弹窗里那行说明改成了五个按钮的提示气泡（底栏是常驻一条，长文字会把状态行挤掉）。
实测底栏在最窄窗口（960px）里用掉 951px，所以再加第六个按钮就会挤 —— 用例已经把
"最小宽度下不重叠、不越界"钉住，以后加按钮会立刻红。

> 说明一下我为什么改回来：上一轮按你说的"作成一个按钮，点开弹窗可以设置"把运行操作也放进了
> 弹窗，但用起来那一页大半是空的、开跑还要多点两下。你现在这条更贴合实际使用。

### Windows 上"闪终端"（已修，2026-09-20）

**根因**：打包产物是**窗口程序**（spec 里 `console=False`），而 `CreateProcess` 在父进程没有
控制台时会**给子进程新建一个控制台窗口** —— 窗口程序每起一个子进程就闪一下黑框。

| 你看到的时机 | 起的是什么 |
| --- | --- |
| 打开应用闪一下 | 环境自检的三个版本探测（`opencode --version` / `bash --version` / `shellcheck --version`，还有 `opencode auth list`） |
| 与模型对话时闪 | 后端 agent 的 CLI 进程 |
| 生成/对话期间 | 长驻的 `opencode serve` —— 它其实是**一直挂着一个黑框**，不只是闪 |
| 执行脚本时 | Git Bash、`taskkill`、shellcheck |

**修法**：新增 `tu_shell_agent/procflags.py`，六处起子进程的地方统一带上
`CREATE_NO_WINDOW`（要按整棵树杀的再按位或 `CREATE_NEW_PROCESS_GROUP`）＋ `SW_HIDE` 兜底；
非 Windows 不加（那些 flags 在别的平台是非法值）。

**顺带加了一条源码级契约用例**：AST 扫遍整个包，任何 `subprocess.run/Popen` 没带这个助手就红 ——
漏一处的表现是"偶发闪一下终端"，人工 review 查不出来。变异验证：五处各去掉一处都会转红。

> 重建后应该完全不闪了。若还有某条路径会闪，告诉我"什么时候闪"，我能按上面这张表直接定位
> 到是哪一个子进程。

### Windows 上"环境变量里有、就是自动获取不到"（已修，2026-09-20）

两个真实成因，都在本机模拟出来了：

1. **进程的 PATH 可能是旧的** —— Windows 在登录/启动时复制一份 PATH。你在"系统属性"里刚加完目录，
   **已经在跑的 explorer.exe 还是旧环境**，从它双击启动的 GUI 自然拿不到新目录；而你的终端是新开的，
   所以终端里有、应用里没有。注册表里的 `Path` 才是当前设置 —— 现在两边合并后再查找。
2. **`shutil.which` 依赖 `PATHEXT`** —— npm / scoop / pipx 装的是 `.cmd` 包壳，没有扩展名可查时
   `which` 要靠 `PATHEXT` 才知道试 `.CMD`；被服务或上层程序启动时它可能被清掉。现在名字没扩展名时
   **显式**按 `.exe → .cmd → .bat → .ps1 → .com → 原名` 逐个试，不依赖 PATHEXT。

顺带解决第三条（界面层）：**查到了什么你看不到**。组件路径三栏原来只有占位文字"留空则从 PATH 中查找"，
"没装"和"没找到"长得一模一样。现在：

- 设置页「组件路径」多了 **「自动检测」按钮**：把三个组件在当前环境里找到的真实路径填进**空栏**
  （你手填过的不覆盖），提示行写明"已自动填入：…"；
- **环境自检完成时也会顺带填一次** —— 打开设置页就能看到自动获取到了什么。

> 重建后请试：设置 → 组件路径 → 点「自动检测」。应该能看到类似
> `已自动填入：opencode → C:\Users\...\AppData\Roaming\npm\opencode.cmd；Git Bash → C:\Program Files\Git\bin\bash.exe`。
> 如果仍然是"没找到"，把「环境自检」页那段文字发我 —— 里面会列出三个组件各自找到没有。

### 为什么"选了 codeagent 还是检测不了模型"（已修，2026-09-20）

我把你那条路径在本机原样跑了一遍，复现出来了：**你在设置页改了「后端 agent」下拉，但没按「保存」。**
而取模型与跑任务用的都是**已保存**的后端 —— 设置页那个下拉只是"待保存的编辑"。于是界面看着像已经切到
codeagent 了，实际仍按 opencode 取模型（这台机器上没用 opencode），模型列表一直空着、状态停在
"正在获取可用模型…"。你看到的就是"检测不了模型"。

现在界面会把这件事说清楚（生效规则不变）：
- 设置页后端那一行的提示会写：`⚠ 尚未保存：下面这些改动还不生效，当前实际使用的是「opencode」。点「保存」之后才会切换。`
- 对话面板取模型时会写：`「设置」里选了 codeagent 但还没保存：取模型仍按当前生效的「opencode」进行。保存后重试。`
- 取到 0 个模型时也会给一句能行动的话：`没有取到任何模型：该后端没有返回模型列表。（若还未登录，先跑一次登录命令；也可以直接在模型一栏手输模型名）`

**已保存 = codeagent 时的实测结果**（本机跑出来的）：
- 设置页模型候选：`sonnet` / `opus` / `haiku`
- 对话面板模型候选：`sonnet` / `opus` / `haiku` / `重新获取可用模型`
- 状态行：`codeagent 不提供模型列表：可用候选 sonnet、opus、haiku；也可直接输入完整模型名。`

所以重建后请这样试：设置 → 后端 agent 选 `codeagent` → **点「保存」** → 右列「模型对话」点开模型下拉。
如果那时还是空的，告诉我你在**哪一处**点的"检测"（设置页那个「检测可用模型」按钮，还是对话面板的
模型下拉），以及状态行/提示行的原文 —— 两处现在都会把原因写出来。

### 你要的"自己的 agent"：内置后端（直连模型 API）已做好（2026-09-20）

你说"输入问题要停顿很久才有回复""不像现在这样有思考过程""我要做的是自己的 agent，
也可以不用调用别的后端"。慢和看不到思考的原因很具体：

| | 首字延迟的来源 | 思考过程 |
| --- | --- | --- |
| opencode | 首次要起 `opencode serve` + 建会话（几秒） | 由 CLI 决定，界面拿不到 |
| claude / codeagent | **每轮对话都要起一个 node 进程**（冷启动几秒） | 只有 CLI 自己的 stream-json 里有 |

新增后端 **「内置 agent（直连模型 API）」**，它在后端下拉里排在 opencode 后面：

- **一个 HTTP 请求**代替"起进程"，首字延迟只剩模型自己的首 token；
- **思考过程看得见**：DeepSeek 的 `reasoning_content`、Anthropic 的 `thinking_delta` 都逐字显示，
  对话记录里自动分成「—— 思考过程 ——」与「—— 回复 ——」两段；
- **模型列表是真的**：可以 `GET {base}/models` 现取（「检测可用模型」和对话面板下拉共用）——
  你之前抱怨的"检测不了模型"在这一路上不存在；
- **不需要装任何东西**，也不依赖 opencode / claude / codeagent；只有一条要求：填 API 地址、key、模型名。
- 安全模型**更窄**：它没有任何工具（不执行命令、不读写文件），产物只有文本；脚本仍然要过
  shellcheck 与人工确认才执行。

在「设置 → 后端 agent」里多三行：**API 地址**（例 `https://api.deepseek.com/v1`）、
**API key**（按密码显示；留空则读 `DEEPSEEK_API_KEY` / `OPENAI_API_KEY` / `ANTHROPIC_API_KEY`）、
**接口风格**（OpenAI 兼容 / Anthropic 兼容）。选到内置后端时这三行可编辑，其它后端下置灰。

**验证**：新增 17 条用例（流式、思考分段、契约、历史、取消、真实模型列表、错误提示、注册表接线），
外加一条端到端 —— 假模型 API + **真实** run_loop / shellcheck / bash：两轮各只发一次 HTTP（不起进程）、
第一轮被 SC2045 拦下、第二轮执行成功。

### 需要你在 Windows 上确认的（我这边看不到）

1. **弹窗在 150% 缩放下放不下**：弹窗固定 860×560，1080p + 150% 时有约 720 逻辑像素高，
   理论够用；若你的屏幕上它贴边或被裁，告我一声，我按可用高度收敛（与主窗口同一套算法）。
2. **右列页签在你的字体下会不会挤**：页签文字是中文，Windows 字体度量与我这里不同，
   若「模型对话 / 校验与输出」两个页签被截断，告我一声。

### 这次改动的验证

- 整套用例 **466 passed / 2 skipped**（100% 与 150% 缩放各跑一遍，结论一致）；
  应用 `--self-test` exit=0（"外观自检通过"）；Linux 产物重建后自检 exit=0。
- 新增 `tests/test_chat_selection.py` 11 条（选中范围与来源、换行符、菜单项、引用进消息、
  发完即撤、截断、取消引用、不挤输入框、记录区引用、整窗两条链路）；
  `tests/test_script_proposal.py` 加 2 条；`tests/test_theme.py` 加 1 条。
- 变异验证（6 条，全部转红）：撤掉 `install_ask_action` 的接线 → 两条链路用例红；撤掉 `_clamp_quote`
  → 截断用例红；把 `_apply_pending_proposal` 改成直接返回 True → 空操作用例红；撤掉折行 →
  折行用例红；把对话面板最小宽度顶回 240 → 三栏比例用例红；按 sizeHint 折行 → 折行判定用例红。

## 四、如果早上你看到有东西不对

按这个顺序找证据，比"感觉不对"有用：

1. 界面底部状态栏 / 自检页的一行结论（哪里失败会写在那里）；
2. 运行目录里的 `meta.json` 与 `server.log`（opencode 路径）或命令行后端的原始输出；
3. `git log --oneline -10` 看我这晚做了什么，每条提交信息里都写了"为什么"；
4. 直接告我现象，我按"像素/日志/复现"三步定位 —— 这一晚的每个修复都是这么做的。
