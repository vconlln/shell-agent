"""后端条目目录：**一个后端一个文件**。

约定（新加后端照着做即可）：

- 每个文件只放该后端自己的东西：显示名、默认命令、版本探测命令与参数、是否需要 serve、
  命令行旗标约定（含模型旗标与禁用工具清单）、以及返回哪个适配器类的工厂；
- 文件里**不做**注册动作（不 import 注册表、不往全局表里塞自己）；
- 收集与查找由 `registry.py` 负责：新增一个后端 = 新增一个文件 + 注册表里的 `BACKENDS` 加一行。

为什么这样切：后端的差异只有"命令名 + 几个旗标 + 要不要 serve"，把差异放在数据里，
适配器与界面都不必长出 `if backend == ...` 分支；而改动某个后端的权限收敛点时，
要动的文件也只有一个。
"""
