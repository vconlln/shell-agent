# 界面外观：Codex 风格深色主题（设计）

## 目标

界面此前用的是 Qt 原生控件外观（浅色、带立体感），与这个工具的气质不符，也不利于长时间
看报告与脚本。目标：**照 Codex 桌面版的外观做一套深色主题**，让它看起来像同一类工具。

## 令牌来源（不凭印象调色）

从本机 Codex 桌面版的样式表里直接解出来的，路径：
`/opt/codex-desktop/resources/app.asar` → `webview/assets/app-*.css` → `electron-dark` 作用域。

| 用途 | Codex 原值 | 本项目令牌 |
| --- | --- | --- |
| 主背景 | `--gray-900` `#181818` | `bg` |
| 面板 / 编辑器 | `--gray-800` `#212121` | `bg_elevated` |
| 更深一层（只读底、凹陷） | `--gray-1000` `#0d0d0d` | `bg_under` |
| 文字 主 / 次 / 三级 | `#fff` / 70% 白 / 50% 白 | `fg` / `fg_secondary` / `fg_tertiary` |
| 边框 浅 / 常规 / 重 | 4% / 8% / 16% 白 | `border_light` / `border` / `border_heavy` |
| 焦点描边 | `#339cffb3` | `border_focus` |
| 强调 / 成功 / 警告 / 错误 | `#339cff` / `#40c977` / `#fb6a22` / `#ff6764` | `accent` / `ok` / `warn` / `error` |
| 圆角 | 6px / 8px / 胶囊 | `radius` / `radius_lg` |
| 列表行高 | 30px | `row_height` |

## 设计要点

1. **颜色只有一个出口**：`ui/theme.py` 的 `css()`（QSS 用）与 `qcolor()`（QPainter/QPalette 用）。
   界面代码不许再写颜色字面量 —— 深色底上出现浅色残留，几乎都是某处写死了颜色。
2. **去原生感**：改用 Fusion 样式（原生样式自带的立体感不受调色板控制）；按钮不用渐变/阴影，
   只叠一层白表示 hover/按下；圆角统一 6/8px；页签做成胶囊；列表行高 30、无网格线；
   分割器是 1px 细线；滚动条细条、轨道透明。
3. **字体分工与 Codex 一致**：界面文字用系统 sans，**脚本、报告、输出、diff 用等宽** ——
   这些内容是用来核对的，等宽才看得清列对齐与空格。
4. **语义色跟着主题走**：阻断/错误用 `#ff6764`、通过用 `#40c977`、未跑/非阻断用灰
   （`#afafaf`）。diff 的浅绿/浅粉底在 `#181818` 上会变成刺眼色块，改成低饱和深底 + 亮前景。
5. **栏头**：三栏各一行"小号、次级色、字距略宽"的标题（方案与模板 / 脚本与轮次 / 校验与输出）。
   分区感靠栏头与 1px 细线，不靠边框和凹陷。
6. **主操作**：底栏"开始"用白底黑字（Codex 的主按钮约定），其余按钮是 5% 白叠加 + 1px 边框。

## 必须记住的一个坑（已写成测试）

**Qt 把 8 位 hex 解析成 `#AARRGGBB`，不是 CSS 的 `#RRGGBBAA`。** 于是 Codex 的
`#ffffff0a`（4% 白）在 Qt 里是"不透明的黄"，实测渲染成 `#ffff0a`。这个错误 QSS 不报错、
palette 也不报错，界面就是花的 —— 只有截图取色才看得见（本次就是取色抓出来的）。
所以：半透明颜色一律写成 `(r,g,b,a)` 元组，经 `css()` / `qcolor()` 出口使用。

## 验证方式

`tests/test_theme.py` 两层断言：
- 字符串层：样式表里不许出现 8 位 hex；关键令牌与 Codex 原值一致；
- 像素层：离屏画一遍主窗口，对主背景、只读区、主按钮**取色**（`#181818` / `#0d0d0d` / `#ffffff`）。

两个变异体（把半透明令牌改回 8 位 hex、不装主题）都能让对应用例转红。

## 范围外

- 亮色主题、跟随系统切换：本次不做。
- 布局重构（顶部标题栏、左侧导航、右侧检查器）：本次不动骨架，保持已被测试锁住的三栏结构。
- 图标、动效、字体打包（等宽字体仍是"探测本机可用族"，找不到就退回 monospace）。


## 修订（2026-09-19，用户反馈后）

用户看完第一版提了两点：**"颜色尽量显得高级，尽量圆角"**、**"缩放有问题，不能调节竖向的长度"**。

### 1. 色板精修（不再是 Codex 原值）

照抄的是 Codex 的**结构与比例**（分层关系、字号、行高、三级文字），色值改成有意精修：

| 令牌 | 第一版（= Codex） | 现在 | 为什么 |
| --- | --- | --- | --- |
| `bg` | `#181818` | `#101114` | 冷调更深；"高级感"来自低对比分层，不是把面板刷亮 |
| `bg_elevated` | `#212121` | `#17181c` | 面板只比底色亮一档 |
| `bg_under` | `#0d0d0d` | `#0b0c0e` | 只读底再深一档 |
| `fg` | `#ffffff` | `#ecedf0` | 纯白在高对比深底上偏刺眼/廉价 |
| `accent` | `#339cff` | `#7c9cff` | 偏靛蓝，比"互联网蓝"收敛 |
| `error` | `#ff6764` | `#f2707a` | 语义色降饱和，红色留给真问题 |
| `radius` / `radius_lg` | 6px / 8px | **10px / 14px** | "尽量圆角"；页签改胶囊 |

三栏与底栏各是一张"卡片"（`QWidget#paneCard`：大圆角 + 极淡边框）—— 圆角要看得出来就得有
边框，而栏与栏的分隔感也由它提供（分割条平时透明，只在悬停/拖动时显色）。

### 2. 缩放：两个真实缺陷

- **分割条抓不住**：第一版把 `QSplitter::handle` 的宽/高写成 1px，视觉上像 Codex 的细线，
  但鼠标抓不住。现在用 `setHandleWidth(8)` 在**代码里**设死（不能只靠 QSS：样式表没加载时
  会退回 Qt 默认的 4px，而"能不能用鼠标操作"这种属性不该依赖样式表）。
- **上下两块比例不可调**：三栏区与"历史+设置"区原本是 `QVBoxLayout` 的 1:1，根本不是分割器，
  怎么拖都不可能变。现在改成"上下竖向分割器 + 历史/设置横向分割器"，四个分割器都能拖。
- 另外各栏内部控件的 `minimumSizeHint` 加起来有 400~500px，会**锁死**分割器比例（拖了没反应），
  所以给每块设了可接受的最小高度（140~200px），压小了靠滚动条看。
- **尺寸记忆**：拖完写进设置（`AppSettings.layout`，可读的 JSON 尺寸而非 `saveState()` 字节），
  下次开窗还原。还原必须放在 `showEvent` 里 —— 控件没显示前几何尺寸未定，构造期 `setSizes()`
  会被夹成平均分布（实测：构造期设 `[397,649]`，显示后变 `[238,238]`）。

### 3. 这一轮新增的测试（都做了变异验证）

`tests/test_ui_layout.py`：所有分割器把手 ≥6px、上下能拖（压得下去/顶得上来）、尺寸存得进
设置且还原、坏掉的布局串不致命、把手宽度不依赖样式表；`tests/test_theme.py` 增加"圆角真的画出来了"
（取角落与中心两个像素比较：角落应露出父底色）。


## 修订二（2026-09-19）：缩小窗口时内容被压没

用户反馈："当我缩小窗口的时候，里面的栏目也跟着挤压了，最后会挤压到看不见。"

实测复现（离屏，窗口高 560px）：

| 控件 | 900px 高时 | 560px 高时 |
| --- | --- | --- |
| 方案预览 | 58 | **12** |
| 右栏报告树 | 171 | **35** |
| 右栏输出区 | 171 | **35** |
| 轮次时间线 | 174 | 60 |

根因：这些内部控件**没有最小高度**，QVBoxLayout 会把它们一路压到近零（固定高度的控件，
比如 64px 的补充要求框，反而不受影响 —— 所以越挤越不均匀）。

修法两条：
1. **关键内部控件声明最小高度**：方案预览 90、脚本/diff 视图 140、时间线 80、对话记录 120、
   报告树 110、输出 110、notes 80、历史列表 90、历史详情 60。
2. **栏与窗口声明最小尺寸**：每栏最小宽度 240~300、最小高度 220~260；窗口最小 960×700
   （= 三栏最小宽 + 两条 8px 把手 + 边距）。低于这个尺寸这个界面本来就放不下，与其让 Qt 把内容
   裁掉，不如让窗口管理器直接不许缩到那么小 —— 平铺窗口管理器也会读 min-size 提示。

修完实测：窗口缩到 960×700 时，方案预览 90、报告树 110、输出 110、脚本 299、历史 90，
没有一块被压没。测试里加了三条（窗口最小尺寸、控件最小高度声明、下限尺寸下内容仍可见），
两个变异体（撤掉预览的最小高度、撤掉窗口最小尺寸）都能转红。

**仍然存在的边界**（诚实写明）：平铺窗口管理器如果无视 min-size 提示、强行把窗口压到更小，
Qt 只能**裁切**（内容被切掉一部分）而不是继续压扁。要彻底解决得把每栏内容包进滚动区域，
本轮没做 —— 因为那会改变三栏"填满窗口"的手感，收益不明显。


## 修订三（2026-09-19）：排布重做 —— 底部「工具区」页签 + 可折叠区块

用户反馈："有些地方如果尺寸太小是否可以增加折叠起来呢，或者你的排布需要重新修改一下……
可以把有些栏目放到环境自检和设置这一横行，用按钮点开进行修改，这样可以节省空间。"
裁定结果：**方案 A（只做底部工具区页签）+ 折叠默认展开可记忆**。

### 新排布

```
┌──────────┬───────────────┬───────────────┐
│ 方案与     │ 脚本与轮次     │ 校验与输出     │   ← 主区三栏，各只干一件事
│ 运行参数   │ （本轮/对比）  │ 报告/输出/取舍 │
├──────────┴───────────────┴───────────────┤
│ [历史运行][模板库][模型对话][环境自检][设置] │   ← 工具区（原 sidePages 演进）
├──────────────────────────────────────────┤
│ 开始 取消 继续修复 改后重跑 │ 状态文字        │
└──────────────────────────────────────────┘
```

- `sidePages`（环境自检/设置两页）→ **`toolTabs`**：历史运行 / 模板库 / 模型对话 / 环境自检 / 设置。
- `templatesPane` 从左列**移进工具区**；左列不再上下堆两块，改叫「方案与运行参数」。
- 模型对话从中栏第三个页签**移进工具区**（`MainWindow.chat_panel`）。
- 右栏三块（校验报告 / 执行输出 / 模型取舍说明与假设）各自包进 `CollapsibleSection`：
  **默认展开**，点标题行收起，状态写进 `AppSettings.collapsed_sections` 并还原。
- 分割器从四个变成三个（main / vertical / center），布局记忆的键同步更新。

### 过程中发现并修掉的两个真问题

1. **工具区最小高度装不下最高的页签**：实测对话面板 `minimumSizeHint` 305px，而工具区最小
   只有 200px —— 内容溢出被裁，症状是输入框盖在记录区上。改成工具区最小 300px、窗口最小
   960×780（高度下限按"最高的常看页签装得下"定）。测试 `test_tool_area_is_tall_enough_for_its_tallest_tab` 守着。
2. **`QPlainTextEdit:read-only` 在页签里不再稳定命中**：对话记录区（只读）渲染成了输入框的底色。
   改成对 `#chatTranscript` **显式**指定更深的只读底 —— "只读区看起来和输入框一样"会让人
   以为可以直接在里面打字。

另外：取色验证改了两处方法。一是**先切到目标页签**再取色（未选中的页签不渲染）；
二是对页签内的控件改用**控件自己的帧**（`widget.grab()`），因为整窗帧按坐标取色会被页签裁切
与相邻控件遮挡影响 —— 这条用例第一版就是被这两点坑了两次。


## 修订四（2026-09-19）：页签的圆角原来根本没生效

用户指出："工作区这里，历史运行、模板库、模型对话等等，这几个按钮不是圆角的。"

**根因**：Qt 画页签时，**圆角半径 >= 页签高度的一半就整个退回画直角**。我按 CSS 习惯写的
`border-radius: 999px`（"胶囊"写法）在这里不是"更大的圆角"，而是**完全没有圆角**。

实测（页签高 28px）：

| 半径 | 8 | 10 | 12 | 14 | 16 | 17 | 20 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 是否圆角 | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |

修法：`radius_pill` 从 `999px` 改成 **11px**（页签高 24~34px 时都安全），页签最小高度提到
24px。工具区与中栏的页签栏都跟着生效。

**为什么测试没发现**：原来的主题测试只断言 QSS 字符串里有 `border-radius`、以及面板/按钮/
只读区的渲染色 —— 页签这条从来没取过色。现在加了 `test_tab_corners_are_actually_rounded`：
拿**选中的**页签（只有它有填充色），比较它的左上角像素与填充色，相同就是直角。
把 `radius_pill` 改回 `999px` 这条用例会立刻转红。

**教训（写下来别再犯）**：凡是"画出来才知道对不对"的样式，都要有一条取色断言；
字符串层面正确的 QSS 可能在 Qt 里被静默忽略。


## 修订五（2026-09-19）：输入控件的底色是矩形 —— 三个叠在一起的原因

用户指出："虽然按钮是圆角，但是按钮底色还是长方形的。"

排查过程（每一步都有取色证据）：

1. **先排除了页签与按钮**：把像素降采样成字符图后，选中页签与主/次按钮的填充在上下边缘都
   明显收窄 → 它们是圆的。审计脚本（遍历所有控件、比较四角与中心）列出真正是"矩形填充"的
   是输入类：`QComboBox`(263×13)、`QSpinBox`(263×13)、`QPlainTextEdit`。
2. **控件被压扁**：左栏内容需要 576px（最小 474），实际只分到 383px → 表单把下拉/微调框压到
   **13px 高**（sizeHint 29px）。而 13px 高时圆角半径 10px ≥ 半高 6.5px，
   **Qt 会整个退回画直角**（和页签那次是同一条规则）。
3. **输入框与面板同色**：输入底 `#17181c` == 卡片底 `#17181c`，所以圆角处的像素与填充同色 ——
   即使真画了圆角也看不出来。

三处一起修：

| 修法 | 内容 |
| --- | --- |
| 表单可滚动 | 新增 `widgets/scroll.py`，左栏 / 模板库 / 设置页 / 自检页的**表单进滚动区**：空间不够时滚动，而不是压缩控件（`test_left_pane_scrolls_instead_of_clipping_when_short` 守着） |
| 输入更深 | `bg_input` `#17181c` → `#121317`（比卡片深一档，圆角才立得住；`test_input_surface_differs_from_the_panel_so_rounding_is_visible` 守着） |
| 圆角验证 | 新增隔离窗口测试：五种控件（下拉/微调/单行/多行/按钮）在正常高度下角像素必须 ≠ 填充色 |

**一个被撤掉的改动（记下来）**：中途给单行输入加过 QSS `min-height: 24px` 防压扁，但变异测试
证明"撤掉它测试照样全绿" —— 因为容器小于最小尺寸时 Qt 反正会挤压，真正管用的是滚动区 +
窗口/栏目的最小尺寸。而且 QSS 的 `min-height` 会**覆盖** `setMinimumHeight()`（实测把方案预览
的下限从 90 冲成 34）。所以这条规则被删掉，不留"看着有用、其实没有"的样式。

另外，取色验证在这轮被坑了两次，方法也修了：**滚动区裁切**会让 `mapTo()` 的坐标落到别的控件上
（取色取错对象），以及 **`widget.grab()`** 对被滚出视野的控件会返回空白帧。现在凡是输入类的
圆角与底色，都在**隔离窗口**里验证。


## 修订六（2026-09-19）：右栏折叠改为按可用高度自动判定

用户裁定："这里如果尺寸足够就不用折叠，这里不需要手动去折叠，请你根据尺寸自动判断是否需要折叠。"

改法：删掉"点击标题手动切换 + 状态写进设置"那一套，改成**纯自动**：

- 优先级固定 **校验报告 > 执行输出 > 模型取舍说明与假设**。理由：运行中盯着的是"有没有问题、
  跑成什么样"；取舍说明是事后核对方案约束时才看的，空间不足时它先让位。**校验报告永不自动收起**。
- 判定逻辑抽成纯函数 `plan_collapse(available, minimums, keep_expanded)`（好测），
  在 `RightPane.resizeEvent` 里执行；带 16px 缓冲带，拖分割条时不会在边界上抖。
- 标题行从 `QToolButton` 换成 `QLabel`：它只显示状态（▾/▸）与原因（tooltip），不再接收点击 ——
  可点的标题会让人以为"这里要手动操作"。
- `AppSettings.collapsed_sections` 字段随之删除（不再持久化折叠状态）。

实测（真实操作触发，右栏高度 → 自动收起）：

| 右栏高 | 自动收起 |
| --- | --- |
| 484px | 无 |
| 370px（窗口缩到最小） | 模型取舍说明与假设 |
| 260px（再拖竖向分割条） | 执行输出 + 模型取舍说明与假设 |
| 拖回大尺寸 | 自动展开回来 |

测试用**真实操作**（缩窗口 + 拖竖向分割条）触发，不直接改控件尺寸 —— 右栏挂在布局里，
直接 `resize()` 会被布局覆盖（这条测试第一版就是这么失败的）。三个变异体（永不自动收、
优先级反了、只收不展开）都能转红。

**操作教训**：回滚变异体时我用了 `git checkout <file>`，把该文件里**未提交**的正经改动一起
回滚了（right.py）。变异体一律用 `cp` 备份/恢复，不用 git checkout。


## 修订七（2026-09-19）：只读视图滚动条用了错颜色 —— "圆角旁的长方形黑尖"

用户描述："A（框的四个圆角旁）不是露出一点浅色，是黑色深色的圆角旁边，有一点点长方形的尖尖黑色。"

排查（这次先复现、再写能失败的测试，不再凭猜测动样式）：

1. 复现：在真实 RightPane 里给输出框灌 300 行触发滚动条，然后对**整条滚动条区域做颜色普查**。
2. 结果：`#0b0c0e`（只读底）1129 px、`#212124`（滑块）123 px、**`#121317`（可编辑输入框底色）117 px**，
   散布在 y=5..147；另有 70 px 的页面底色（那是圆角处应有的）。
3. 根因：`QPlainTextEdit:read-only QScrollBar { background: … }` 这种**后代选择器**里，Qt 把伪状态
   判在**滚动条自己**身上（滚动条永远不是只读）→ 匹配不到只读规则，落到
   `QPlainTextEdit:!read-only QScrollBar` → 只读输出框的滚动条被涂成"可编辑输入框"的颜色。
   两者只差一档（#0b0c0e vs #121317），所以在深色底上看起来就是一小块长方形深色。

修法：滚动条轨道**按控件名点名**（`#outputView` / `#scriptView` / `#notesView` / `#chatTranscript` /
`#compareView` / `#planPreview` → 只读底；`#extraInstructionEdit` / `#chatInput` → 输入底），
不再依赖伪状态。修复后普查：`#121317` 从 117 px 降到 **0**。

测试 `test_read_only_view_scrollbar_uses_the_read_only_surface`：断言滚动条区域里**不允许出现**
输入框底色 —— 这条**能失败**（把选择器改回 `:read-only` 形式立刻转红），与上一轮那条
"两种写法都通过"的假测试不同。

**方法教训**：上一轮我加过一条滚动条轨道规则并写了测试，但那条测试两种写法都通过 ——
说明我当时的取样点没落在真正出问题的地方（我只取了条带中段，问题像素在滑块上下与边缘）。
这次改成**整条区域颜色普查**（按颜色计数、看 y 范围），一次就定位到了。


## 修订八（2026-09-19）：外观可配置 —— 界面缩放、字体、亚克力模糊

用户要求："在设置里面增加界面缩放和设置字体的设置，再增加亚克力模糊。"

设置页新增「外观」分组，四个字段（都存在 `AppSettings` 里、随设置文件持久化）：

| 设置 | 字段 | 行为 |
| --- | --- | --- |
| 界面缩放 | `ui_scale`（0.8–1.6，默认 1.0） | **同时**放大字号与所有尺寸（圆角、行高、内边距、滚动条宽度、栏头字号）。只放大字号会让界面变挤。颜色不缩放。 |
| 界面字体 | `ui_font`（默认空 = 系统默认） | 下拉列出 `QFontDatabase.families()`；空值不塞 `font-family`，交给 Qt/系统。 |
| 等宽字体 | `mono_font`（默认空 = 自动探测） | 作用于脚本视图/报告/输出/diff/对话记录。 |
| 背景效果 | `backdrop`（off / translucent / blur） | 见下。 |

### 亚克力模糊：能做与不能做，都写清楚

**真正被模糊的是窗口背后的内容**，这只有窗口管理器/合成器能做，Qt 没有跨平台接口。所以分两步：

1. **半透明**（任何平台都能做）：`WA_TranslucentBackground` + 底色带 alpha（面板 92%、只读底 82%）。
   文字与强调色**不做透明**——那会直接毁掉对比度（测试里锁住了这一条）。
2. **模糊**（能拿到才做，`ui/backdrop.py`）：
   - Windows 11 22H2+：`DwmSetWindowAttribute(DWMWA_SYSTEMBACKDROP_TYPE=38, 3=Acrylic)`，
     顺带 `DWMWA_USE_IMMERSIVE_DARK_MODE` 把标题栏也切深色；
   - KDE/X11：`_KDE_NET_WM_BLUR_BEHIND_REGION`（本环境没有 xcb 绑定 → 如实返回 False，
     不去 shell 调 xprop，免得在用户机器上留下莫名其妙的子进程）；
   - Wayland（Niri/sway/GNOME）：**没有给普通应用的模糊接口** → False；
   - macOS：NSVisualEffectView，本 PySide6 环境不支持 → False。

拿不到模糊时**不假装成功**：`blur_available=False`，状态栏与设置页如实写"当前桌面不支持窗口
模糊，已退化为半透明"。这条断言是**对着平台的真实能力**比的（直接调 `try_enable_blur`），
而不是对着窗口自己报的标志——第一版就是拿标志当基准，于是"把标志写死成 True"这个变异体照样通过。

具体到这台机器（Arch + Niri/Wayland）：**模糊拿不到，只能半透明**。Windows 上应当能拿到真正的亚克力。

### 热更新

`MainWindow.apply_appearance()` 统一负责装主题 + 窗口半透明 + 平台模糊；生产入口 `app.py` 在建窗口
后调它，设置保存后 `_on_settings_saved` 也会调它 —— 改缩放/字体/背景**不需要重启**。

### 测试（三个变异体都能转红）

`tests/test_appearance.py`：缩放同时改字号与尺寸（含**像素级**"按钮真的变高了"）、默认缩放不改版式、
字体落到样式表、默认背景完全不透明、半透明时底色带 alpha 且**文字仍不透明**、模糊可用性不谎报、
四个字段存取往返。变异体：缩放只改字号 / 默认改半透明 / 谎报模糊可用 —— 分别让对应用例转红。


### 性能事故（记下来）：设置页的字体下拉把测试从 47s 拖到 332s

加完外观设置后整套测试从 47s 涨到 **332s**。定位过程：

1. 先怀疑字体枚举 → 加进程级缓存 `@lru_cache` 的 `font_families()`：**没用**（27.3s → 27.4s）。
2. 再怀疑 `setStyleSheet` 重新 polish 所有窗口 → 给 `apply_theme` 加**幂等短路**（内容相同就不设）：
   **也没用**。
3. 用 `--durations` 看每条用例：单条 1.6~4.3s，说明代价在"建一次窗口"这条路径上。
4. 真凶：设置页构造时给两个下拉各插入 **2106 个字族**（4200+ 次 `addItem`），而每个 MainWindow
   都会建一个设置页。

修法：字体列表**延迟到设置页真正显示时才填**（它平时躲在工具区页签里，根本不需要）。
效果：外观测试 27.4s → **6.4s**，整套 332s → **60s**。

顺带保留的两处改动（各自独立成立，不是这次的主因）：字族按进程缓存、`apply_theme` 幂等短路
（后者对生产也有意义：切页签/重建窗口时不必重新 polish 整棵树）。


## 修订九（2026-09-19）：半透明的真正拦路虎是**层数**，不是 alpha

用户连着两轮反馈"主工作区的黑底与浅黑底没有透明效果""还是没有完全透明，对话框之类的还是纯黑底
和浅黑底"。前两轮我都在调 alpha（92% → 78% → 窗口 43%），都没解决 —— 因为问题不在单层透明度。

### 怎么测出真相："双背景法"

把界面分别渲染到**品红**与**纯绿**两张画布上，同一像素的红通道差值为 `(1-α)·255`
（表面自身颜色被消掉，剩下的就是真实合成不透明度）。于是得到一张"等效不透明度图"：

- 探针脚本先量到主工作区 **95~97%**，而窗口空白处只有 43%（= 窗口 CSS 的 alpha）——
  说明内容区下面还压着别的东西；
- 再逐层量**祖先链自身的底色**，抓到真凶：`QSplitter → QStackedWidget → QTabWidget →
  折叠区块 → 容器 QWidget` 每一个都在重刷一遍窗口底色。**43% 叠四到六层 = 97%**。

### 三处修法（缺一不可）

1. **容器一律不上色**：QSS 里给 `QSplitter / QStackedWidget / QScrollArea / QTabWidget::pane /
   CollapsibleSection / #sectionBody / 各页签页` 写 `background: transparent`，底色只在最外层画一次。
2. **调色板不重复着色**：`build_palette` 在半透明模式下把 `Window / Base / AlternateBase /
   Button / ToolTipBase` 置为全透明（QSS 已经上过色，调色板再来一遍就是两层）。
3. **视口不重复着色**：Qt 给 `QAbstractScrollArea` 设了 QSS 底色后，视口会再画同一层
   （实测 51% 被叠成 74%），`unstack_viewports()` 把视口底色交还外层。

另外两个被同一病根连累的地方：`thin_containers()` 连容器的调色板也清掉；
**对话框是独立顶层窗口**，`WA_TranslucentBackground` 只设在主窗口上时，弹出的确认框仍是一整块
纯不透明深色 —— 现在 `backdrop.apply_to()` 统一处理主窗口与所有对话框，确认框在构造末尾调用它。

### 修完的实测值（双背景法，等效不透明度）

| 区域 | 修前 | 修后 |
| --- | --- | --- |
| 窗口空白处 | 43% | **43%** |
| 方案预览 | 96% | **46%** |
| 对话记录 | 87% | **61%** |
| 脚本 / 输出 / 报告视图 | 95~97% | **71%** |

内容区保留约七成不透明是**刻意的**：那里是等宽代码与诊断报告，全透会让文字糊在壁纸上。
空白与卡片区域透出一半以上，观感上"透"是明确成立的。

测试：`test_rendered_surfaces_let_the_backdrop_through`（整窗渲染 + 坐标映射取样，
断言每个表面至少透出 18%、至多 75%）、`test_opaque_mode_keeps_every_surface_solid`
（off 模式不许被透出来）、`test_dialogs_follow_the_translucent_backdrop`
（对话框要带 `WA_TranslucentBackground`，关掉后新开的对话框要回到不透明）。


### 修订九补记：页签条那一条"方角底色"

用户圈出「工具区」的页签条：整条底色是直角矩形。实测原因 —— `QTabBar` 原来没写底色，
由调色板自绘（Fusion 风格填充 + 类框架），条内等效不透明度 **69%**、条外只有 46%，
所以它是一条比周围更实、更亮的方角块。改法：给 `QTabBar` 显式写 `background-color: bg_elevated`
与 `border-radius: radius_lg`，圆角就与下面的面板对得上（页签本身仍是胶囊）。

测试 `test_tab_strip_background_is_rounded`：条内取"页签上方的内边距"处的纯底色，
断言它是半透明的（不是调色板刷的不透明块）且与角落不同色。变异体验证：撤掉那条
`border-radius`，用例立刻转红。


## 修订十（2026-09-19）：亚克力模糊不必等系统支持 —— 自己模糊壁纸

用户问："亚克力模糊为什么要系统支持呢，不用系统支持你可不可以支持亚克力模糊？"

### 为什么"真亚克力"要系统支持

要模糊的是**窗口背后**的像素，而那些像素只有合成器拿得到（Windows 的 DWM、KWin、
Hyprland 的 blur 插件）。应用自己读不到别的窗口：Wayland 出于安全根本不允许，
X11 抓屏拿到的也是"整块屏幕"而不是"窗口背后"。

### 但观感可以自己造

壁纸图片是能读的，而窗口背后通常就是壁纸。于是新增 `ui/acrylic.py`：

1. **找壁纸**：设置里指定的（图片或目录）→ DankMaterialShell 的会话状态
   （`~/.local/state/DankMaterialShell/session.json` 的 `wallpaperPathDark`）→ KDE 的
   `appletsrc` → hyprpaper 配置 → `~/Pictures` 里最新的一张；
2. **模糊**：按半径大幅降采样再逐级放大（mipmap 式）。两个被否掉的做法：
   `QGraphicsBlurEffect` 在**离屏场景里不生效**（实测接缝处仍是纯黑，半径 40/80 都没变化）；
   在 Python 里做可分离卷积又太慢（百万级像素的循环）。现在的实现全程在 Qt 的 C++ 里，
   1400×900 实测 **0.07s**，结果按 (路径, mtime, 尺寸桶, 强度) 缓存，换壁纸/改强度才重算；
3. **铺到窗口最底层**：`MainWindow.paintEvent` 先画模糊壁纸，**再**让 QSS 的半透明深色底
   压上去 —— 顺序反了就是一张普通背景图，不是毛玻璃。尺寸变化时按新尺寸重算（分桶缓存）。

### 说清楚的代价（不假装它是真亚克力）

- 背后如果是别的窗口，它不会出现在这一层里 —— 这是静态壁纸，不是实时抓屏；
- Wayland 拿不到窗口的屏幕坐标，模糊层按窗口大小重新裁切，**不保证与桌面壁纸逐像素对齐**
  （模糊之后这点错位基本看不出来）；
- 找不到壁纸时退化为普通半透明，状态栏与设置页都如实写明白。

设置页新增两个旋钮：「亚克力壁纸」（留空 = 自动找，带"选择图片…"）与「模糊强度」（0~120 px），
背景效果下拉里新增「亚克力模糊（自绘壁纸，不需要系统支持）」，与原来那条
「亚克力模糊（问系统要，Windows 有）」并存 —— 两条路各自成立，谁都不冒充谁。

测试（`tests/test_acrylic.py`，9 条）：找壁纸的四种来源、目录取最新、找不到返回 None、
模糊真的把硬边摊开（而两端保持原色）、没有壁纸就退化为半透明并如实提示、切走模式要撤掉那一层、
两个字段存取与设置页回填。判据用的是"同一扇窗分别渲染到品红与纯绿画布"：亚克力下两张图必须
**逐像素相同**（壁纸层不透明），半透明下必须不同。


## 修订十一（2026-09-19）：浮层不参与透明化 —— "看不见字"的三种成因

用户反馈："选择亚克力模糊的时候，选择字体的背景也跟着透明了，看不见字"。

追下来其实是**三件不同的事**凑在了一起，每一件都得单独修：

### 1. 下拉弹层整块透明（主因）

实测：把界面渲染到品红/纯绿上反推，**设置页里的**下拉，其弹出列表底色与壁纸同色
（行底色 = 189，文字 236 → 对比度 **1.7:1**）；而独立的下拉却是正常的深色。
原因：弹层是独立顶层窗口，会继承页面的调色板（页面在透明模式下是 transparent）。
另外**真正要上色的是"弹层容器"那一层**（QComboBox 的 QFrame 弹窗），只给列表上色没用。

修法：`theme.py` 里加"浮层守卫"（`_PopupKeeper`）—— 在 `Polish/Show` 事件上把底色钉死，
并在 `apply_theme` 时对已存在的浮层先钉一遍；浮层单列令牌 `bg_menu`（非 off 模式 alpha 244 ≈ 96%）。
判据用"窗口类型字段"精确判断弹层：`Qt.WindowType.Popup` 的位里含 `Window`，
直接位与会把所有顶层窗口判成弹层（实测把主界面列表也染成浮层色，六个用例转红）。

### 2. 输入类表面不够不透明

收起状态的下拉（字体名就写在那里）在亮壁纸上的对比度只有 **3.1:1**（可读线 4.5:1）。
所以 `bg_input` 从 51% 提到 80%：输入框/下拉/数字框是读字与写字的地方，透明让给大块的只读内容区。
"内容区要明显透"那条用例相应改成断言输入类的**下限**。

### 3. 亚克力层本身太亮

真亚克力的配方是"模糊 + **色调** + 噪点"，我第一版只做了模糊：本机壁纸平均亮度 **189**，
浅色文字直接压上去当然糊。补上色调层（59% 深色），壁纸仍看得出来，字也压得住。

三处都有测试：`test_combo_popup_stays_readable_in_transparent_modes`（弹层在亮底上的对比度 ≥ 4.5:1，
撤掉守卫立刻转红）、`test_acrylic_layer_is_dark_enough_for_light_text`、
以及输入类 alpha 的下限断言。


## 修订十二（2026-09-19）：界面文案规范 —— 书面语，不用口语

用户要求："亚克力模糊去除问系统要……以后不要出现这种口语化的文字在软件里面"。

**规则**：界面上出现的文字一律用书面语；不用口语词，不加括号旁白，不用"其实/多半/不假装/
挑一个/问系统要"这类表达。技术名词（DWM、合成器、PATH、shellcheck）保留原样。

本次改动：

| 位置 | 改前 | 改后 |
| --- | --- | --- |
| 背景效果 | 亚克力模糊（问系统要，Windows 有） | 亚克力模糊（系统提供） |
| 背景效果 | 亚克力模糊（自绘壁纸，不需要系统支持） | 亚克力模糊（界面自绘） |
| 等宽字体 | 自动（挑一个可用的等宽字体） | 自动选择（等宽字体） |
| 系统模糊提示 | 当前桌面多半不支持，保存后会退化为半透明（不模糊） | 由系统合成器提供模糊。Windows 11 22H2 及以上版本可用；不可用时自动退化为半透明。 |
| 自绘模糊提示 | 自绘模糊：没找到壁纸图片，请在上面指定一张（否则只有半透明） | 未找到壁纸图片，请在「亚克力壁纸」中指定；未指定时使用半透明。 |
| 状态栏 | 模糊不可用：当前桌面不支持，已退化为半透明 | 系统模糊不可用，已退化为半透明 |
| 组件路径占位 | 留空 = 用 PATH 里找到的 opencode | 留空则从 PATH 中查找 opencode |

用例 `test_appearance_text_is_formal` 把四种模式名称钉死，并对提示/状态文字做口语黑名单检查；
变异体验证：把标签改回"（问系统要，Windows 有）"立刻转红。


## 修订十三（2026-09-19）：面板圆角外的"深黑色角"

用户圈出「校验与输出」栏里几处"深黑的色角"。用像素普查定位，**不是圆角没生效**，
而是有两层东西在圆角外面露出了既不属于面板、也不属于它背后那层的颜色：

1. **纯容器在画窗口底色**：折叠区块的外壳与 `#sectionBody` 这类纯布局容器被 Qt 标了
   `WA_StyledBackground`；`不透明` 模式下没有对应 QSS 规则时，Qt 用调色板的 Window 色
   （窗口底色 `#101114`）填充它们 —— 比卡片 `#17181c` 更深，于是每个面板圆角外露出一圈楔形。
   修法：`CollapsibleSection, #sectionBody { background: transparent }` **在所有模式下生效**
   （之前这条规则只在半透明模式下启用）。
2. **表头 `::section` 自带底色**：`QHeaderView::section` 写的是窗口底色，而表头是列表/树的
   **子控件，不会被父控件的圆角裁剪** —— 校验报告树的左上/右上角因此露出深色方块。
   修法：`QHeaderView, QHeaderView QWidget { background: transparent }` 且 `::section` 改为透明，
   底色交给下面的圆角面板自己显示（只保留底部分隔线）。

### 判据（通用，不针对某一处）

`test_no_rounded_panel_shows_foreign_colors_at_its_corners`：把整窗渲染出来，对十个圆角面板
逐个数它们角上 9×7 像素的颜色，只允许出现**它自己的底色、它背后的底色、以及两者与描边的混色**。
普查结果：修前六处不合格，修后全窗 0 处。另有一条针对用户截图那三块面板的定向断言。


## 修订十四（2026-09-19）：系统模糊拿不到时改用自绘，而不是退回"没有模糊"

用户发来截图："亚克力模糊（系统提供）现在成了这样的" —— 整窗一片平的深色，看不出毛玻璃。

实测（双背景法）：该模式下本机合成不透明度 73%，与「半透明」完全一致，状态栏也如实写着
"系统模糊不可用，已退化为半透明"。也就是说**行为没错，但退化目标选错了**：
用户要的是毛玻璃，系统给不了的时候应该用**我们自己的自绘模糊**顶上，而不是把他丢回
一个没有模糊的半透明界面（自绘这条能力上一版就已经做好了）。

改法（`MainWindow.apply_appearance`）：

1. 选「系统提供」时先问平台；拿不到就把**生效模式换成 `acrylic`**，并按自绘那条路装配
   （窗口不透明 + 去叠加 + 浮层不透明），`_acrylic_fallback = True`；
2. `_refresh_acrylic()` 与壁纸轮询在这面标志下同样生效 —— 于是系统模糊不可用时，
   自绘层照样铺、壁纸换了照样跟着换；
3. 提示语如实区分三种结果：系统模糊可用／改用界面自绘模糊／连壁纸都没有才退化为半透明。
   设置页的下拉提示也写明了这条链路。

测试：原有的"拿不到模糊要如实报"用例改为断言**真拿到了才用系统模糊**（窗口保持半透明、
不铺自绘层），拿不到时断言已切到自绘；另加一条：有壁纸→自绘接管，无壁纸→半透明。
全套 336 passed / 1 skipped。


## 修订十五（2026-09-19）：重影、合并亚克力选项、文案再收一遍

用户反馈三件事："半透明又成这种重影的了"、"系统提供和界面自绘没有区别了"、
"不要出现『生成脚本用』的文字，不要口语化"。

### 1. 重影 = 半透明窗口没有擦除重绘区域

半透明窗口（`WA_TranslucentBackground`）的底色带 alpha，正常绘制是**混合**而不是**覆盖**：
滚动或重排后只重绘一块区域时，上一次留在后备存储里的像素会透过来 —— 屏幕上就是重影。
实测：把窗口渲染一次，滚动 40px 后**只重绘滚动区**，与"全新渲染"相比有 6 万多个采样点不同。

修法两条一起用：

- `backdrop.erase_damage(widget, event)`：在 `paintEvent` 里先把这次要重绘的区域用
  `CompositionMode_Source`（替换，不受 alpha 影响）擦成透明，再走正常绘制；
  **画完必须 `painter.restore()`** —— 合成模式是画笔状态，留在 Source 上会让后续所有绘制
  都变成"替换"，半透明整体失效（实测：留在 Source 上时窗口渲染不再与背景混合）；
- 任何滚动条一动就 `window.update()`（`_wire_scroll_repaints`）：整窗重绘 + 上面的擦除，
  才能保证那块区域回到"全新绘制"的样子。

顺带一个测量方法上的教训："品红/绿双画布"那套反推 alpha 的办法**被擦除破坏了**（画布颜色会被
一并擦掉，两种底色渲染结果永远相同）。改用**渲染到透明画布直接读 alpha** —— 更直接也更准。

### 2. 合并两个亚克力选项

用户说得对：在拿不到系统模糊的机器上，"系统提供"与"界面自绘"完全一样，那是冗余。
现在只有三种背景效果：**不透明 / 半透明 / 亚克力模糊**；亚克力的模糊来源**按可用性自动选择**：
系统合成器（Windows 11 22H2+ 的 DWM）→ 界面自绘 → 半透明。老设置里的 `blur` 在读取时
归一到 `acrylic`（`AppSettings.normalize()`，`load()` 也要调 —— 直接 setattr 会绕过
`__post_init__`，第一版就漏在这里）。

另外补上：亚克力模式下**连壁纸都没有**时，生效模式退化为 `translucent`（重新打开窗口半透明），
而不是留一块不透明的深色。

### 3. 文案

- 设置页那一栏改为：分组「脚本生成」/ 行名「模型」/ 说明"用于生成 shell 脚本；
  模型对话的模型在「模型对话」面板中单独设置……"（"生成脚本用"这种以"用"结尾的口语标签删除）；
- 同一批清扫：对话面板占位与状态文字（"输入问题，例如：……"、"本对话使用的模型：……"、
  "当前会话：……"）、"上一条提问尚未返回"、"该目录未记录会话，无法恢复"等；
- 口语黑名单加上被点名的短语：`生成脚本用`、`测试替身`、`还没回复完`、`可以继续问` 等。


## 修订十六（2026-09-19）："半透明没有透明效果" —— 属性设晚了

用户报："你这半透明都没有透明效果了啊"。渲染层面量不出问题（透明模式的合成 alpha 是正常的
110~187），所以问题在**真实窗口**上：`WA_TranslucentBackground` **必须在窗口被创建之前设好**，
窗口已经在屏幕上时再改这个属性不会生效 —— Qt 的窗口后端早已定型。

而合并亚克力选项时我把亚克力改成了"窗口不透明"（当时是为了绕开浮层继承透明度的问题）。
于是用户的设置被归一成 acrylic → 启动时创建的是**不透明窗口** → 在界面上切回「半透明」时
属性虽然设上了，窗口却还是不透的 ✓ 症状与用户描述完全一致。

三处改动：

1. `_set_translucent(window, want)`：属性真的需要变、且窗口已经在屏幕上时，**隐藏再显示一次**，
   强制按新属性重建窗口（Qt 的标准绕法）。下次显示前设属性的常规路径不受影响。
2. 亚克力也保持**半透明窗口**：自绘那层模糊壁纸本来就不透明、铺上就看不到桌面，所以观感不变，
   但「半透明 ↔ 亚克力」互切不再需要重建窗口。浮层可读性早就由 `_PopupKeeper` 单独钉住，
   不再依赖"窗口不透明"这个前提。
3. `erase_damage` 从"擦成透明"改为"**重填窗口底色**"：填透明会把 Qt 在 paintEvent 之前画好的
   窗口底色一并抹掉，边角处就成了没有背景的窟窿。填自己的底色既清掉了旧像素，也不会挖洞。

用例：`test_switching_background_modes_actually_toggles_window_translucency`（逐个模式断言窗口
属性与可见性，并断言切回半透明后自绘层已撤）。全套 343 passed / 1 skipped。


## 修订十七（2026-09-19）：Windows 上两个"透明"效果都不生效 —— 改成界面自绘

用户报："半透明和亚克力模糊在 win11 上都没生效"。查证后是**两条系统能力在真机上都不成立**：

1. **Windows 的 `WA_TranslucentBackground` 必须搭配无边框窗口**才生效
   （Qt 的已知限制；[StackOverflow 上这个问题](https://stackoverflow.com/questions/75432262/how-to-make-wa-translucentbackground-work-on-windows-without-using-framelesswindo)
   的答案是 Windows 上做不到）。我们保留原生标题栏 → 客户区永远不透明。
2. **Windows 11 的 DWM 亚克力要求窗口没有重定向位图**（`WS_EX_NOREDIRECTIONBITMAP`）——
   [VLC 的提交](https://mailman.videolan.org/pipermail/vlc-commits/2025-January/070907.html)
   正是为这个改的。Qt 的普通窗口有重定向位图，所以 `DwmSetWindowAttribute` 即使返回成功，
   那层背景也显示不出来。

于是**改成两端统一的自绘底色**，不再问系统：

| 模式 | 底色层 |
| --- | --- |
| 不透明 | 无（主题纯色） |
| 半透明 | 壁纸**不模糊** + 淡色调 `TRANSLUCENT_TINT`(alpha 96) |
| 亚克力模糊 | 壁纸**高斯模糊**（强度可调） + 浓色调 `TINT`(alpha 150) |

- 窗口**始终不透明**：`backdrop.py` 里删掉了 `enable/disable_translucent`、
  `try_enable_blur`、`_enable_windows_acrylic`、`_enable_kde_blur`（都成了不可达代码），
  文件顶部写清了为什么；
- 没有壁纸 → 生效模式退回 `off`（纯色）并如实说明，不留一块假的透明；
- `erase_damage` 填的必须是**不透明**色：窗口不透明之后，填带 alpha 的色会在重绘处挖出窟窿；
- `_window_background_color()` 有底色层时返回色调色，纯色模式返回主题底色。

连带的测量方法变化：窗口不再透明，"渲染到透明画布量 alpha"这套判据失效（永远 255）。
新判据是**颜色差异**：半透明模式下同一位置的颜色必须与纯色模式明显不同（壁纸透出来了），
以及**模糊程度差异**：用硬边壁纸时亚克力的过渡像素必须显著多于半透明。

用例：`test_backdrop_modes_paint_a_self_drawn_layer`、
`test_no_wallpaper_falls_back_to_a_solid_background`、
`test_translucent_is_sharp_and_acrylic_is_blurred`、
`test_rendered_surfaces_let_the_backdrop_through`（改判据）、
`test_switching_background_modes_switches_the_layer`、`test_dialogs_follow_the_current_backdrop_mode`。
整套 347 passed / 1 skipped。


## 修订十八（2026-09-20）：主区重新分工 —— 对话上右栏、工具区进「控制台」弹窗

用户反馈（原话）："你不觉得这个界面的工具区有点太占用空间了吗，软件的主要作用是写 shell 脚本，
如果你能在模型对话之后，实时显示修改的代码，用绿色红色等等标出来，并且询问用户是否接受这个代码，
像 cursor 的 agent 那样，这样是最好的" 以及 "模型对话放到右边，然后工具区可以放到底部，
'开始、取消、继续修复'这里，作成一个按钮，点开工具区就出现弹窗可以设置，你懂我意思吧，
并且对话也可以选中代码进行对话，询问代码，并且增加修改后是否接受"。

### 新排布

```
┌──────────┬───────────────────┬──────────────────────┐
│ 方案与     │ 脚本与轮次         │ [模型对话][校验与输出] │  ← 右列是页签：
│ 运行参数   │ （本轮/对比上一轮） │  对话与校验报告同栏     │     对话在右（用户要求）
├──────────┴───────────────────┴──────────────────────┤
│ [控制台]  就绪                                        │  ← 底栏：一个按钮 + 状态
└─────────────────────────────────────────────────────┘

「控制台」弹窗（860×560）：[运行][历史运行][模板库][环境自检][设置]
  └ 运行页：开始 / 取消 / 继续修复 / 改后重跑 / 打开运行目录 + 一句说明
```

- **模型对话从底部工具区移到右列**（`rightTabs`：`模型对话` / `校验与输出`）。一栏放两件事而不是
  再加一栏：高 DPI 的小屏上四栏会窄到没法用。
- **工具区整体搬进 `consoleDialog`**（QDialog）：低频面板不再常驻占高度，主区三栏拿到全部高度。
  底栏只留 `consoleButton`（"控制台"）与状态文字；`focus_tool_tab(page)` 负责"某个面板该在哪里显示"。
- 删掉主窗口那条竖向分割器（三栏区 / 历史与设置区）：它已经没东西可分了。设置里遗留的
  `vertical` 键被 `_restore_layout` 忽略（只认它知道的键），不会把老用户的布局弄坏。
- 右栏栏头 `paneHeader` 消失：右列的标题由**页签**承担（`模型对话` / `校验与输出`）。

### 模型提议（像 Cursor 那样）：红绿 diff + 三个决定

模型回复里出现脚本且与中栏不同 → 中栏切到**差异页**显示红删绿增（流式期间 400ms 节流刷新），
对话面板出现一行摘要与三个按钮：**接受 / 接受并重跑 / 拒绝**。

- 「接受」只把脚本放进中栏，不执行；
- 「接受并重跑」= 接受 + `verify_edited`（shellcheck → 人工确认 → 执行），**闸门一个都不少**，
  快捷的是点击次数；
- 「拒绝」中栏一字不改。

### 选中代码 → 引用 → 提问

用户要求"对话也可以选中代码进行对话，询问代码"。做法是复用用户已经在做的动作：**选中、右键**。

- `ui/widgets/selection_menu.py`：给只读文本控件装一条置顶菜单项（`createStandardContextMenu()`
  之上插一项，不自己实现复制/全选）。`QTextCursor.selectedText()` 的换行是 U+2029，要转成 `\n`。
- 中栏「本轮」与「对比上一轮」都能选中提问，来源写成 `本轮脚本 · 第 6–9 行`（单行写 `第 6 行`）；
  对话记录区内部也支持，来源是 `对话记录`。
- 引用显示在输入框上方的一条引用栏里（可「取消引用」），**原样**进入发给模型的那条消息
  （`~~~` 围栏，避免被脚本里的三个反引号提前闭合），并在对话记录里可见 —— 不做隐藏注入。
- 一次消息一份引用：发出去即撤，不粘在下一条问题上；超 120 行或 6000 字符按上限截断，
  且在消息里写明"引用已截断"。

### 过程中踩到并写进测试的坑

1. **未选中的页签不渲染**：右栏两个页签、控制台五个页签里只有一个在渲染。所有像素取色用例
   都要先 `setCurrentWidget`（或在弹窗里 `open_console(page)`），否则 `mapTo` 拿到的是过期几何，
   取到的是**别的控件**的颜色 —— 角测试、按钮缩放测试、圆角测试都因此假红过一次。
2. **`QMenu.exec` 不能被 monkeypatch**：PySide6 的 C++ 方法赋值会被静默忽略（实测），
   测试在模态菜单里挂死（用 faulthandler 抓到栈顶就是 `menu.exec`）。改成模块级
   `exec_menu()` 这一层可替换的接缝。
3. **引用栏必须显式上色**：不上色的 QWidget 会被通用 `QWidget { background-color: bg }` 填成窗口
   底色，在面板里像一道凹陷的黑条（与折叠区块"深黑色角"同源）。

### 右列变宽之后连带发现的两个尺寸问题

1. **右列的最小宽度把左栏挤扁**：对话面板里两个下拉写死了 240 / 200 的最小宽度，
   整栏 `minimumSizeHint` 被顶到 599px —— 1440 宽的窗口里右列吃 599、左栏只剩 284
   （意图是 320）。下拉改成 150 / 110（缩窄时显示走省略号），右列最小宽度降到 ~385，
   实测三栏回到 [321, 563, 522]。用例：`test_default_columns_leave_room_for_the_left_pane`。
2. **窄栏里底部控件行重叠**：底部那行（发送/取消/把最新脚本放进中栏 + 模型下拉 + 可用模型）
   自然宽度约 490px；窗口缩到 960 时右列只有 386px，`QHBoxLayout` 的反应是让模型下拉
   **盖住**「可用模型」按钮（实测坐标重叠）。新增 `ui/widgets/wrap_row.py`：
   放不下就折行（宽的时候仍是一行、模型选择仍贴右端），折行时**整组**折，
   别把「模型」标签留在上一行末尾。三个前提都写进了代码注释：
   - 折行判定要按控件的**最小占位**算（`QComboBox` 的 sizeHint 只有 38px，但
     `setMinimumWidth(110)` 之后布局压不下去，"算得下、排出来溢出"就是这么来的）；
   - 要覆盖 `minimumSizeHint()`，否则布局把"当前这一行"的宽度写成控件最小宽度，折行永不触发；
   - 外层布局要 `SetNoConstraint`，否则 Qt 会把布局最小尺寸设成控件最小尺寸（同上）。
   用例：`tests/test_wrap_row.py` 4 条 + `tests/test_chat_panel.py` 2 条；
   变异验证：撤掉折行、把最小宽度顶回 240、按 sizeHint 折行 —— 三条都转红。
   默认三栏比例同时调成 `[320, 560, 520]`：右列比原来只放校验报告时宽一点，
   且底部那行控件在 1440 窗口里一行放得下。

### 测试

新增 `tests/test_chat_selection.py`（11 条：选中范围与来源、换行符、菜单项只在有选中时出现、
引用进消息/发完即撤/截断/取消引用/不挤输入框、记录区引用、整窗两条链路），
`tests/test_script_proposal.py` 加 2 条（接受并重跑确实走 `verify_edited`、无提议时是空操作），
`tests/test_theme.py` 加 1 条（引用栏取色）。布局相关的旧用例按新排布重写：
`test_ui_layout.py`（右栏折叠改为按**窗口高度**触发）、`test_dpi_layout.py`（工具区 → 控制台弹窗）、
`test_appearance.py`（弹窗里的控件要先显示弹窗、逐帧取色）、新增 `tests/test_wrap_row.py`（4 条）。
整套 **463 passed / 1 skipped**，应用 `--self-test` exit=0，Linux 产物重建后自检 exit=0。


## 修订十九（2026-09-20）：用「150% 缩放」模拟 Windows 形状，揪出一条真 bug

用户睡觉前的要求是"确保 windows 版本无误"。开发机是 Linux，真机行为没法直接验，
所以能做的就是**把 Windows 的形状在本机复现出来**，再让用例去撞。

### 复现手段

`QT_SCALE_FACTOR=1.5`（或 2）跑同一套用例：逻辑屏幕像素会按比例缩小
（offscreen 测试屏 800x800 → 150% 下只有 533x533），凡是"按屏幕可用区收敛"的规则
都会走进小屏分支 —— 这正是 Windows 150% 缩放（1080p 只有 720 逻辑像素高）的形状。

### 撞出来的真 bug：最小尺寸可以比屏幕还大

`window_minimum_for()` 里那个"舒适下限"是 `max(720, …)`，屏幕更小时它照样返回 720：

| 屏幕可用区 | 旧公式 | 新规则 |
| --- | --- | --- |
| 1920×1080 | 960×780 | 960×780（不变） |
| 1280×720（1080p/150%，用户机） | 960×612 | 960×612（不变） |
| 800×800（本机测试屏） | 720×680 | 720×680（不变） |
| 533×533（150% 模拟） | **720×520（比屏幕还大）** | 506×506 |
| 640×480 | **720×520（比屏幕还大）** | 608×456 |

"最小尺寸比屏幕还大"正是用户报过的那三条症状的根因：窗口管理器照给最小尺寸，
布局只能违反它 —— 文字出框、面板与输入框重叠、某栏被压没。
新规则：下限只在屏幕装得下时生效，否则收敛到可用区的九五成（宽 758 / 高 547 以下才触发，
所以大屏与用户机的取值逐值不变）。

**修法**：`window_minimum_for` 改成 `_pick(available, floor, comfortable, ratio)`
（先取"下限与舒适值之间的目标"，再用屏幕的九五成封顶）；
新增 `console_dialog_size_for`（同一套规则，「控制台」弹窗原来固定 860×560 ——
1366×768 笔记本在 150% 下只有 512 逻辑像素高，弹窗比屏幕还高，底部「关闭」按钮点不到）。
弹窗在 `open_console()` 里**装不下才收敛**，用户自己调过的尺寸不动。

用例：`test_window_minimum_never_exceeds_a_small_screen`、
`test_console_dialog_size_never_exceeds_a_small_screen`、
`test_open_console_resizes_itself_to_fit_the_screen`；
原来那条 `test_window_minimum_keeps_a_usable_floor` 把两条**互相冲突**的要求同时钉住了
（640×480 的屏幕上不可能既 ≥720×520 又 ≤ 屏幕），已改写为"屏幕装得下时才保留下限"。

### 连带修的测试方法问题：取色没算 devicePixelRatio

150% 下 `widget.grab()` 出来的是**物理像素**图（1.5 倍），而所有取色用例都在用逻辑坐标
`image.pixel(x, y)` —— 于是取到了别的地方（实测："脚本只读底"那一格取到了行号区的颜色，
三条用例假红）。新增 `tests/conftest.py::Grab`：按 `devicePixelRatioF()` 换算后再取色，
`test_theme.py` 与 `test_appearance.py` 的取色全部改走它。改完之后 150% 与 100% 两套
跑出来的结论一致 —— 这也是"像素级用例在高 DPI 下仍然可信"的前提。

### 其它 Windows 方向的核对（都做了，结论写在 packaging/build.md）

- **打包收集**：Linux 产物用**同一份 spec**，PyInstaller 分析表里 `wrap_row` / `selection_menu`
  都在（新模块都在包内，自动收集），模板库由 `BUILTIN_TEMPLATES` 在首次 `list()` 时写进用户数据
  目录 —— 打包产物不会出现空模板库；
- **CRLF**：`set_quote()` 显式把 CRLF 规范化成 `\n`（Qt 控件那条路本来就是 U+2029 → `\n`，
  但公开入口不能假设调用方经过控件），用例 `test_crlf_quote_is_normalized`；
- **打包脚本**：`build.bat`（纯 ASCII + CRLF）与 `build.sh` 共用同一份 spec、抽查同一组 Qt 插件，
  这两条都有契约用例守着；本次没有新增数据文件，两个脚本都不需要改；
- **手测清单**：`packaging/build.md` 新增 §6.2（B1~B9：控制台弹窗、右列页签、选中引用、
  接受并重跑、窄栏折行、150% 缩放），以及"高 DPI 形状怎么在没有 Windows 的机器上验"的说明。

### 测试

`QT_SCALE_FACTOR=1.5` 跑完整套：**466 passed / 2 skipped**（跳过的两条：需要真 opencode 凭据的
live 冒烟、以及本机测试屏装不下右列对话页的那条，后者跳过理由写在用例里）。
默认（100%）跑完整套：**466 passed / 2 skipped**。


### 附：顺手清掉的不可达平台代码（同一晚）

`ui/backdrop.py` 里还留着 `enable_translucent` / `disable_translucent` / `try_enable_blur` /
`_enable_windows_acrylic`（DWM）/ `_enable_kde_blur`（KWin），以及**重复定义了两遍**的
`erase_damage` —— 这是改自绘方案时的合并残留（文件中间那段注释写着"这里曾经有…"，
但函数本体还在下面）。产品里对它们的引用数是 0，也就是说：

- 它们是**Windows 专有、开发机上跑不到、也没有用例**的代码，正是"Windows 版本可能出问题"
  最典型的一类来源；
- 模块文档字符串还在描述旧方案（"拿不到模糊时不假装成功"、"Windows 走 DWM"），与现状不符。

处理：删掉这五个函数与重复定义，文档字符串改写成现状（两种效果都是界面自绘，窗口始终不透明，
并保留"为什么不再问系统要模糊"的实测原因）；`theme.py` 里两处过时说明同步改掉。
`test_ui_layer_keeps_its_platform_branches_in_one_place` 的豁免名单从两个文件**收紧到一个**
（只剩 `ui/acrylic.py` 的 Windows 壁纸探测）—— 变异验证：往 `ui/chat.py` 或 `ui/backdrop.py`
里塞一个 `sys.platform` 分支，这条用例立刻转红。

顺带修掉一条**脆弱的取色断言**：`test_theme.py` 里"主按钮浅底深字"与"引用条底色"用的是
**单个像素**，而那个像素正好在文字上 —— 别的用例改过全局字体之后采样点落进字形，
断言就红了（实测取到 `#b7b8ba` / `#9e9ea2`，也就是文字的颜色）。改成
`tests/conftest.py::Grab.dominant()`：取区域内**出现次数最多**的颜色（底色必然面积最大），
这类断言从此与字体度量无关。


## 修订二十（2026-09-20）：用户实测三条 —— 控制台点不开、模型没在同一行、可用模型要列表

用户带着截图报的三条（原话）："点了控制台没反应，并且把这个模型，移动到跟发送在同一行，
可用模型点开用梳状的列表呈现"。

### 1. 点了控制台没反应（真 bug，我自己的接线错）

```python
self.console_button.clicked.connect(self.open_console)      # ← 错在这里
```

`clicked` **总是带一个 `checked=False`**，于是调用变成 `open_console(False)`；
`open_console` 里写的是 `if page is not None:` —— `False is not None` 成立，于是
`tool_tabs.indexOf(False)` 抛 TypeError，Qt 打印异常、**槽函数中断、弹窗永远不出现**。

为什么之前的用例没抓到：那些用例都是**直接调** `open_console(...)`，没有一条走"真点击"。
修法两层：
- 接线改成 `lambda: self.open_console()`（显式吞掉 checked）；
- `open_console` 内部把非控件实参一律当成"没指定页"（`isinstance(page, QWidget)`），
  免得以后再有人直接连方法又踩一遍。

新增 `tests/test_signal_wiring.py` 把这一类问题**通用化**：
- `test_clicked_slots_do_not_take_positional_arguments`：AST 扫出所有
  `<X>.clicked.connect(self.方法)`，用 `inspect.signature` 检查目标方法**不许有位置参数**
  （带默认值的也算 —— `page=None` 照样中招）。这是"带默认值的参数看起来安全、其实不安全"
  那条教训的机器化版本；
- `test_console_button_click_opens_the_dialog`：**真点一下**按钮，断言弹窗出现、关闭生效；
- `test_open_console_tolerates_the_checked_flag`：退一步，直接传 `False` 也不许崩。
变异验证：接线改回 `connect(self.open_console)` → 转红；去掉 `isinstance` 守卫 → 转红。

### 2. 模型与发送同一行（这是需求，不是 bug）

底栏那一行原来需要 491px，而右列的最小宽度只有 385px —— 于是 WrapRow 折成两行，
模型掉到第二行（用户截图里就是那样）。要"永远一行"就得把这一行的**固有宽度**压到 385 以内：

| 项 | 修前 | 修后 | 理由 |
| --- | --- | --- | --- |
| 发送 / 取消 | 52 / 52 | 52 / 52 | 不动 |
| 把最新脚本放进中栏 | 143 | **存入中栏** 78 | 完整含义进提示气泡；标签是这一行里最大的一块 |
| 模型标签「模型」 | 26 | **去掉** | 下拉的占位文字"使用会话模型"与提示气泡已经说明它是什么 |
| 模型下拉 | 110 | 130 | 反而**加宽**了：要看全模型名 |
| 可用模型按钮 | 78 | **去掉**（并入下拉，见下条） | 底栏少一个按钮才塞得下 |
| 合计 | 491 | **330** | 右列最小宽度 385，实得 365 → 稳定一行 |

实测（模拟三种屏幕、窗口取最小尺寸）：右列 385 / 对话 383 / 控件行 365 / 需要 330 → **一行**。
WrapRow 仍然保留作兜底（窗口被压到比最小尺寸还小时折行而不是重叠），
用例改成"把面板强行压到 260px"来触发折行。

### 3. 「可用模型」点开就是列表

原来：点「可用模型」按钮把候选塞进下拉，再点下拉才看到列表（两步）。
现在：**列表就在模型下拉里** —— 点开下拉＝"我要挑模型"，这时去要一次可用模型列表
（末项固定「重新获取可用模型」，选中它只重新取列表、**不会**被当成模型名发出去）。
三个细节都是踩过/量过才定的：
- **TTL 300s**：取列表在 opencode 上是起子进程，连点几下下拉会同时跑好几个；
  而且列表是异步回来的，正在打开的弹层被清空重填在平台上可能自己关掉。
  想强制刷新有末项。用例：连点两次只问一次。
- **弹层比下拉宽**：原来弹层宽度跟着 130px 的下拉走，`deepseek/deepseek-v4-flash` 被截成
  `deepseek/deepse…` —— 而"选哪个"恰恰要看清全名。现在按最长模型名撑开（上限 520px 与屏幕九成）。
- **手输仍然可用**：下拉保持可编辑（CLI 后端常要手输完整模型名），这一条有用例钉住。
- 控制器侧去重：同一时刻只跑一个取列表的线程，并给出"正在获取可用模型… / 已取到…"的状态文字。

用例：`test_opening_the_model_dropdown_asks_for_the_model_list`、
`test_model_list_is_offered_in_the_dropdown_and_sets_the_model`、
`test_refresh_item_asks_again_without_becoming_the_model`、
`test_model_can_still_be_typed_by_hand`、
`test_opening_the_dropdown_twice_does_not_hammer_the_backend`、
`test_model_popup_is_wide_enough_to_read_full_names`；
`test_model_picker_sits_in_the_bottom_button_row` 改成**在面板最小宽度下**断言"同一行"。

### 测试

整套 **547 passed / 2 skipped**（100% 与 150% 缩放各跑一遍，结论一致）；
应用 `--self-test` exit=0。


## 修订二十一（2026-09-20）：输入控件又变成直角了 —— "半径 >= 半高"这条老规律再次咬人

用户截图报："你看看会话这里，他不是圆角的黑底"（会话下拉）。

### 根因（量出来的，不是猜的）

输入类控件的圆角写的是 `radius_lg`（14px），而这些控件**没有 min-height** —— 高度完全由字体
度量决定，正好卡在 Qt 那条退化规则上：**圆角半径 >= 控件高度的一半，整个退回直角**
（页签那条注释里记过一次同样的行为）。实测：

| ui_scale | 输入控件自然高度 | 半径(radius_lg) | 结果 |
| --- | --- | --- | --- |
| 0.8 | 26px | 11.2px | 圆角（勉强） |
| 1.0 | 29px | 14px | 圆角（只差 0.5px） |
| 1.4 | 35px | 19.6px | **直角** |
| 1.6 | 39px | 22.4px | **直角** |

1.0 档在 Linux 上只是"勉强圆角"，而 Windows 的字体度量更矮（同样的字号下行高更小）→
在用户那台机器上 1.0 档就已经是直角。这也解释了为什么我本机没看到。

### 修法与两个"以后不会再漏"的用例

- 输入类圆角改成 `radius`（10px，与按钮一致；主题文档本来就是"控件 10px、面板 14px"，
  是这条规则自己跑了偏）。各档的 半径/半高 变成 0.62~0.84，都有余量。
- **为什么老用例没拦住**：`test_form_controls_are_rounded_when_they_have_a_normal_height`
  给控件设了 `setMinimumHeight(30)` —— 正好把半径压进合法区间，于是这个 bug 从它眼皮底下过去了。
  新用例 `test_input_corners_stay_rounded_at_natural_height_and_every_scale` **故意不设最小高度**，
  在 0.8/1.0/1.4/1.6 四档下逐档取角（几何 + 像素两条都查）；另加
  `test_every_input_in_the_window_stays_rounded_at_the_default_scale`：把窗口里所有
  单行输入控件（跳过 QComboBox/QSpinBox 内部的文本行）都按"半径 <= 0.45 × 高度"查一遍 ——
  这条守的是**平台字体差异**（Windows 更矮也必须是圆角）。
  半径**从真正生效的样式表里正则读出**，不读令牌名：写死 `sized("radius")` 的话，
  有人改回 `radius_lg` 用例照样绿（变异验证时正是这一点被抓到并改成现在这样）。
- 变异验证：把规则改回 `radius_lg` → 两条用例都转红。

### 顺带修的测试脆弱点

`test_no_rounded_panel_shows_foreign_colors_at_its_corners` 里允许的"描边混色"原来是一个
**写死的实测值** (43,45,51)；半径从 14px 改成 10px 之后角上多出的一像素是
`border_heavy` 与底色的混合色 (50,51,55)，于是失配。改成**按令牌算**：把 `border` 与
`border_heavy` 的 alpha 分别叠在面板底色/背后底色上，得到四个合法混合色，再看采样像素是否
落在"底色↔混合色"的线段上。以后改任何圆角/描边参数都不会再假红。

### 关于"黑底"

用户说的"黑底"是设计如此：主题里输入框比面板**更深**（`bg_input` #121317 vs 面板
#17181c），半透明模式下输入框的 alpha 是 205/255（比内容区更不透明）—— 这是实测定的
（51% 时下拉里的字体名对比度只有 3.1:1，低于可读线；用户报过"看不见字"）。
所以这次只修了"圆角"，底色没有动；想让输入框在透明模式下更透气，得先解决可读性。


## 修订二十二（2026-09-20）：输入框不再"黑底" —— 底色反过来做，并给选择框用按钮浅底

用户第二次报同一处（"黑底为什么不动？…这个会话这里的黑底不是圆角的"）。我先把用户发来的截图
**逐像素量了一遍**再动手，量出来的事实是：

- 会话框的填充是 rgb(19,21,24) ≈ `bg_input`(#121317) + 壁纸透出 —— **它就是输入框的底色**，
  不是原生样式没命中；
- 那张图里会话框的角**是圆的**（填充边缘在 y=20→26 之间从 x≈76 内收到 x≈70），
  半径约 14px、框高 36px。所以我上一轮修的"半径 >= 半高退化成直角"在**那张图里没有体现** ——
  两张图哈希相同，是同一张旧图。

但用户对"黑底"的观感是真的：`#121317` 接近纯黑，在半透明壁纸上就是一块黑洞，
**看不出是个圆角框**。所以按用户选的三条一起改：

| 项 | 改前 | 改后 |
| --- | --- | --- |
| 输入框底色 `bg_input` | `#121317`（比卡片**深**） | `#1b1c21`（比卡片 `#17181c` **亮**一档） |
| 输入类描边 | 白 8%（`border`） | 白 16%（`border_heavy`），hover 24%（新增 `border_strong`） |
| 选择框（QComboBox）底色 | 同输入框 | 新增 `bg_select` = `#232428` = **按钮那种浅底**（白 5% 叠在卡片上的合成色） |

为什么"反过来做"：原设计是"输入框比面板更深，形状靠更深的底色表达"。但底色深到接近纯黑时，
"更深"与"黑洞"只差一线 —— 现在靠"**比卡片亮 + 描边看得见**"表达形状，观感更接近常规控件的
凹陷/浮起关系，也不会在浅色壁纸上糊成一团。半透明模式下的 alpha 维持 205（输入类是读字的地方，
这一条是之前实测定的：再透一点对比度掉到 3.1:1）。

### 新增/加固的用例（都按像素与阈值钉住方向）

- `test_inputs_are_a_lighter_field_not_a_black_hole`：输入框亮度 > 卡片、选择框亮度 > 输入框，
  且**贴边那一列像素要比填充亮 ≥25**（阈值是量出来的：16% 描边差 35，8% 只有 16 —— 取中间值，
  撤掉"加亮描边"就转红）。同时把焦点策略设成 NoFocus，否则量到的是蓝色焦点环。
- `test_editable_combo_has_no_dark_box_inside`：下拉内部不许出现文本框底色。
  **诚实说明**：实测（Qt 6.11 + Fusion）即使去掉 `QComboBox QLineEdit { background: transparent }`
  那条规则，内部也不会出现深框 —— 所以那条规则是防御性的，用例守的是**结果**。
- 变异验证：底色调回 `#121317` → 红；选择框不用 `bg_select` → 红；描边改回 8% → 红。

### 顺带

`test_no_rounded_panel_shows_foreign_colors_at_its_corners` 的允许色集合补上了
`border_strong`：这条用例跑在别的用例之后时，控件可能还带着 hover 状态（Qt 在没有
mouse-leave 事件时不会清 `WA_UnderMouse`，pytest 里没有真实鼠标移动），角上的描边就是那一档。


## 修订二十三（2026-09-20）："会话底下的黑色底色" —— 纯布局容器自己上了色

用户第三张截图圈出会话那一行的左半边（"看见我圈出来的会话底下的黑色底色了吗，把这个黑色底色去掉"）。

### 根因（量出来的）

不是画错了颜色，是**一个纯布局容器在给自己上色**：`#chatSessionRow`（装"会话 + 下拉 +
扫描历史会话 + 新对话"的 QWidget）没有 QSS 规则，落进通用 `QWidget {{ background-color: bg }}`
里，于是整行被盖了一层深色。同一类还有 `#proposalBar`、`#controlsRow`（底部控件行）
与右列的页签容器 `#rightTabs`（右列改成页签时加进去的，透明名单里只加了 toolTabs/centerTabs）。

实测（**均匀**壁纸 + 半透明模式，避免把壁纸本身的明暗当成 bug）：

| 取样位置 | 修前 | 修后 |
| --- | --- | --- |
| 会话行内的空白（x=行内左 5px） | **46** | 68 |
| 面板内不属于任何容器的空白（同一列、行下方 20px） | 68 | 68 |

差 22 亮度，肉眼就是会话那一行底下的一条深色带 —— 因为这一行大部分被下拉与按钮盖住了，
**露在外面的恰好就是「会话」标签周围那一片**，与用户圈出来的位置完全一致。
纯色模式下这层深色与面板底色相同，所以看不出来；只有透明模式才暴露。

### 修法与用例

- 在"任何模式下都不上色的纯容器"规则里补上 `#rightTabs, #chatSessionRow, #proposalBar, #controlsRow`，
  并把 `WrapRow` 的 objectName 设成 `controlsRow`（有名字才点得到）。
- 新增 `test_layout_containers_do_not_paint_their_own_background`：均匀壁纸 + 半透明模式，
  逐个容器量"容器内空白 vs 面板空白"，亮度差必须 ≤6。变异验证：去掉那条规则 → 会话行 46 vs 68 → 转红。
- 顺手把整窗 + 控制台五个页的**所有**纯容器扫了一遍（内部 vs 紧邻外部的亮度差），确认没有第二处。

### 方法上的教训（写下来免得再犯）

用户截图里的"某块更暗"**不能直接当成 bug**：壁纸本身有明暗，同一窗口里左栏与右列的亮度差
可能只是壁纸不同位置的颜色。第一版我用真实桌面壁纸量，得到"右列比左栏暗 30"的结论、
还差点把 `#rightTabs` 当成根因 —— 换成**均匀壁纸**再量，两者一致（都是 68），
问题定位到会话行那一层。**对照实验要控制变量。**


## 修订二十四（2026-09-20）：Windows 上 codeagent 检测不到、也选不了模型

用户睡前报："你的 codeagent 检索模型还是有些问题，我在 windows 上有 codeagent，但是还是检索不到，
也没法选择模型"。开发机是 Linux，所以这一轮的办法是**把 Windows 的形状完整模拟出来**再改。

### 1. 检测：四条"别把装了的东西判成没装"

`.cmd` 包壳那条老修法（经 `cmd.exe /c` 起）本身是对的，但**只解决了"能不能起进程"**，
剩下四种情况在用户机器上依然会表现为"检测不到"：

| # | 修前 | 修后 |
| --- | --- | --- |
| 1 | 只试 `--version`：CLI 不认它就直接判失败 | 依次试 `--version` → `-v` → `version` → `--help`，任一成功即算找到；失败时把试过的参数写进提示 |
| 2 | 退出码 0 但输出里没有版本号 → "无法识别版本" = 判成没装 | **算找到了**（版本记为"未识别"）—— 能不能问出版本与能不能干活是两件事 |
| 3 | 探测没有 stdin 设置：CLI 想读输入（TTY/确认）会挂到 20s 超时，界面上就是"点了检测没反应" | `stdin=DEVNULL`：需要读输入的命令立刻拿到 EOF |
| 4 | 只在 PATH 里找。GUI（双击 exe）拿到的 PATH 常常比终端少几条 —— 用户"明明装了却找不到"多半是这个 | PATH 找不到时再按常见安装目录找一遍：`%APPDATA%\npm`、`%LOCALAPPDATA%\pnpm`、`scoop\shims`、`.cargo\bin`、`.local\bin`、`Program Files\nodejs` 等（`.exe` → `.cmd` → `.bat` → `.ps1` → 无扩展名） |

### 2. 参数传递：npm 包壳要**绕开 cmd**

`.cmd` 只能经 `cmd.exe` 起，而 cmd 会把命令行**再解析一遍**：裸的 `& | < > ^ ( )` 是语法，
`%NAME%` 会展开，**换行是命令分隔符** —— 而生成脚本时 `--append-system-prompt` 的值是**多行**的，
经 cmd 传必然被切碎（这是本轮顺带发现的一条真问题，不只是检测）。

npm 的包壳内容非常固定，它自己就写着用哪个 node、跑哪个 js：

```bat
endLocal & goto #_undefined_# 2>NUL || title %COMSPEC% & "%_prog%"  "%dp0%\node_modules\codeagent\cli.js" %*
```

于是直接读出 `[node.exe, ...\cli.js, *参数]` 交给 `CreateProcess`（标准引号规则，
标点与换行都安全）。读不出这种形状才退回 `cmd.exe /d /s /c`，并且对参数做 cmd 转义
（引号内的 `&` 等是字面量、`%` → `%%`、不加引号时 `^` 转义）。**探测与跑任务共用同一个转换**，
所以不会出现"检测通过、一跑就失败"。

顺带把这段逻辑从"按平台门控"改成"按解析出来的路径门控"（`.cmd`/`.bat`/`.ps1` 本来就是
Windows 专有格式）：这样它在 Linux 上也能被用例钉住，不必等到用户机器上才发现。

### 3. 选模型：命令行后端也要有候选

用户说"也没法选择模型"。查下来是真缺点：**对话面板**的模型下拉对命令行后端只写了一句提示就返回，
下拉里空空如也 —— 用户只能猜模型名。现在把后端自己声明的候选（claude 系：`sonnet` / `opus` /
`haiku`）铺进下拉，仍然可以直接手输完整模型名，并在状态行说明候选从哪来。

### 用例（全部在 Linux 上模拟 Windows，注入平台与假文件系统）

`tests/test_backend_invocation.py` 扩到 16 条：npm 包壳解析成 `[node, cli.js, *args]`、
**多行系统提示词原样送达**、跑任务与探测走同一个转换、PATH 失败时找常见目录、
`.exe` 优先于 `.cmd`、cmd 转义（`& % >` 与换行）、`--version` 失败后试 `-v`、
"能跑但没版本号"算找到、探测传了 `stdin=DEVNULL`、失败提示列出了试过的参数；
`tests/test_backend_selection.py` 加 1 条：命令行后端的对话面板下拉里要有候选、且仍可手输。


## 修订二十五（2026-09-20）：运行按钮回到主界面底栏，控制台挪到最右

用户带截图报（圈出弹窗「运行」页那一行按钮，箭头指向底栏的「控制台」）：
"把控制台里面运行部分的按钮放到主界面和控制台同一行，控制台按钮往右边挪一挪"。

这是对我上一轮改动的**修正**：上一轮按"工具区可以放到底部，'开始、取消、继续修复'这里作成一个
按钮，点开工具区就出现弹窗可以设置"把运行操作也塞进了弹窗；实际用起来那一页大半是空的，
而开跑一次要多点两下。现在：

| 位置 | 内容 |
| --- | --- |
| 主窗口底栏（一行） | `开始` `取消` `继续修复` `改后重跑` `打开运行目录` → 状态文字（占满剩余宽度）→ **最右** `控制台` |
| 控制台弹窗 | `历史运行` `模板库` `环境自检` `设置`（**没有「运行」页了** —— 空的页签不如没有） |

- 弹窗里那行说明（"开始前请在左栏选择方案文档…"）改成五个按钮的**提示气泡**：
  底栏是常驻的一条，长文字会把状态行挤掉，悬停时看得到就够。
- 实测底栏在 1440 宽窗口里用掉 951px/960px（最小窗口宽度），所以**再加第六个按钮就会红** ——
  用例把"最小宽度下不重叠、不越界"也钉住了。

用例：`test_run_buttons_live_in_the_bottom_bar_next_to_the_console`（五个按钮不开弹窗就可见、
与「控制台」同一行、控制台在状态文字右侧且贴右端、弹窗里没有空「运行」页、
最小宽度下底栏不重叠），`test_main_window_has_three_panes_and_tool_tabs` 的页签契约同步更新。


## 修订二十六（2026-09-20）：Windows 上别再闪终端了

用户报："与模型会话，为什么在 windows 上会闪终端，而且应用打开的时候也会闪终端，不要闪终端"。

### 根因

打包产物是**窗口程序**（`packaging/tu-shell-agent.spec` 里 `console=False`），而
`CreateProcess` 在"父进程没有控制台"时会**给子进程新建一个控制台窗口** —— 于是窗口程序每起一个
子进程就闪一下黑框。我们起子进程的地方正好都在用户看得见的时机：

| 时机 | 起的是什么 |
| --- | --- |
| 打开应用 | 环境自检的三个版本探测：`opencode --version` / `bash --version` / `shellcheck --version`（还有 `opencode auth list`） |
| 与模型对话 / 生成脚本 | 后端 agent 的 CLI 进程（claude / codeagent / …） |
| 生成或对话期间 | 长驻的 `opencode serve` —— 不带标志时它会**一直挂着一个黑框**，不只是闪 |
| 执行脚本 | Git Bash、`taskkill`（杀进程树）、shellcheck |

### 修法：一个模块 + 一条源码级契约

新增 `tu_shell_agent/procflags.py`：`no_window_kwargs()` 在 Windows 上给出
`creationflags=CREATE_NO_WINDOW`（按整棵树杀的进程再按位或上 `CREATE_NEW_PROCESS_GROUP`）
外加一份 `STARTUPINFO` 的 `SW_HIDE` 兜底；非 Windows 返回空 dict（那些 flags 在别的平台是非法值）。

六处调用点全部改用它：`shell_toolchain/execute.py`（bash + taskkill）、`shellcheck.py`、
`detect.py`（两处探测）、`opencode_adapter/server.py`（serve）、`opencode_adapter/models.py`、
`agent_backends/cli_agent.py`（探测 + 跑任务）。

**为什么写成契约用例**：漏掉一处的表现是"偶发闪一下终端"，人工 review 查不出来。
`tests/test_no_console_flash.py` 用 AST 扫遍整个包，要求每一处 `subprocess.run/Popen`
要么直接 `**no_window_kwargs()`，要么在同文件里对 `**` 展开的那个字典做过
`.update(no_window_kwargs(...))`；再点名为六条已知路径各写一条断言。
变异验证：把五处里任意一处去掉 → 契约用例立刻转红。


## 修订二十七（2026-09-20）：Windows 上"环境变量里有、就是自动获取不到"

用户报："你这个在 windows 上自动获取环境变量里的组件路径获取不到，windows 有环境变量，
但是仍然无法自动获取"。

### 两个真实成因（都在本机模拟出来了）

1. **进程的 PATH 可能是旧的**。Windows 在登录/启动进程时复制一份 PATH：用户在"系统属性"里刚加完
   目录，**已经在运行的 explorer.exe 仍是旧环境**，从它启动的 GUI（双击 exe）自然拿不到新目录 ——
   而用户的终端是新开的，所以"我明明配了、终端里也有"。注册表 `HKCU\Environment` /
   `HKLM\...\Session Manager\Environment` 里的 `Path` 才是当前设置。
2. **`shutil.which` 依赖 `PATHEXT`**。npm / scoop / pipx 装的 CLI 是 `.cmd` 包壳：名字没有扩展名时
   `which` 要靠 `PATHEXT` 才知道试 `.CMD`；被服务/上层程序启动时它可能被清掉，于是"在 PATH 里
   却找不到"。

顺带还有第三种（界面层）：**查到了什么用户看不到** —— 组件路径那三栏只有占位文字
"留空则从 PATH 中查找"，所以"没装"和"没找到"在界面上长得一模一样。

### 修法

- 新增 `tu_shell_agent/envpath.py`：
  - `effective_path()` = 进程 PATH + 注册表 PATH（用户级在前、展开 `%NAME%`、大小写不敏感去重）；
  - `find_executable()`：名字没扩展名时**显式**按 `.exe → .cmd → .bat → .ps1 → .com → 原名`
    依次试一遍，不依赖 `PATHEXT`；已经是路径就直接看它在不在；
  - 平台判断同时认 `"nt"` 与 `"win32"`（探测层用的是后者；只看 `"nt"` 会让整条逻辑静默失效）。
- `detect.system_deps()` 的 `which` 换成它（`platform` 可注入，方便在 Linux 上钉 Windows 分支）；
  `agent_backends/invocation.resolve_command` 的默认查找也换成它 —— 于是 opencode / bash /
  shellcheck / codeagent / claude 的自动获取走同一条路。
- 设置页「组件路径」加「自动检测」按钮：把三个组件在**当前环境**里找到的真实路径填进**空栏**
  （用户手填过的不覆盖），并在提示行写明"已自动填入：…"。环境自检完成时也会顺带填一次 ——
  这样"自动获取到了什么"在界面上一眼可见。

### 用例（`tests/test_envpath.py`，11 条）

`%NAME%` 展开、注册表 PATH 合并顺序与去重、用户级优先于系统级、非 Windows 不动 PATH、
没有 `PATHEXT` 也能找到 `.cmd`、同名优先 `.exe`、**进程 PATH 是旧的而注册表里有 → 仍能找到**、
绝对路径直查、POSIX 仍按原名查、探测链路真的用了这个实现（注入 `platform="win32"`；
不能改 `os.name` —— 那会让 `pathlib` 去实例化 `WindowsPath` 直接抛异常）、
「自动检测」只填空栏且不覆盖手填值。
变异验证：不合并注册表 PATH / 不补扩展名 / 探测链路改回 `shutil.which` / 自动填入覆盖手填值 —— 四条全红。


## 修订二十八（2026-09-20）：为什么"选了 codeagent 还是检测不了模型"

用户追问："但是为什么还是检测不了模型呢"。在本机把那条路径原样跑了一遍，复现出来了：

**用户改了设置页的下拉、但没按「保存」。** 而"取模型"与"跑任务"用的都是**已保存**的后端
（`RunController._backend_id()`），设置页那个下拉只是"待保存的编辑" —— 于是界面看着像已经切到
codeagent 了，实际仍然按 opencode 去取模型：`opencode models` 当然取不到（这台机器上根本没用
opencode），模型下拉一直空着、状态行停在"正在获取可用模型…"。用户看到的就是"检测不了模型"。

修法（都是把"界面状态"与"实际生效的东西"对齐，不改变生效规则 —— 那条规则是对的）：
1. 设置页的「后端 agent」提示行：选中项与已保存的不一致时，明确写出
   `⚠ 尚未保存：下面这些改动还不生效，当前实际使用的是「opencode」。点「保存」之后才会切换。`
2. 对话面板取模型时同样先说清楚：`「设置」里选了 codeagent 但还没保存：取模型仍按当前生效的
   「opencode」进行。保存后重试。`（并且不再去跑那条注定失败的取模型命令）
3. 取到 **0 个模型**时给一句能行动的话，而不是含糊的"已取到可用模型"或一直停在"正在获取…"：
   `没有取到任何模型：该后端没有返回模型列表。（若还未登录，先跑一次登录命令；也可以直接在
   模型一栏手输模型名）`

顺带确认了"已保存 = codeagent"时的行为是对的（本机实测）：
设置页模型候选 `['', 'sonnet', 'opus', 'haiku']`；对话面板模型候选
`['', 'sonnet', 'opus', 'haiku', '重新获取可用模型']`，状态行写着
`codeagent 不提供模型列表：可用候选 sonnet、opus、haiku；也可直接输入完整模型名。`

用例：`test_unsaved_backend_selection_is_spelled_out`、`test_empty_model_list_is_reported_clearly`
（`tests/test_backend_selection.py`）。变异验证三条全红：对话面板不提示未保存 / 设置页不提示尚未保存 /
空列表照旧含糊。


## 修订二十九（2026-09-20）：自己做 agent —— 内置后端直连模型 API

用户要求（原话）："与模型对话这里，我输入问题，需要停顿很久才能有回复，好像不是直接调用后端
opencode 或者 codeagent，不像现在这样我输入问题有思考过程什么的，我要做的是自己的 agent，
你也可以自己做 agent 不用调用别的后端，当然这是最好。"

### 慢在哪、看不到什么

| | 首字延迟的来源 | 思考过程 |
| --- | --- | --- |
| opencode | 首次要起 `opencode serve` 再建会话（几秒） | 由 CLI 决定，界面拿不到 |
| claude / codeagent | **每轮对话都要起一个 node 进程**（冷启动几秒）+ `--resume` | CLI 自己的 stream-json 里才有，界面上没有 |

### 新增后端：`builtin`（内置 agent，直连模型 API）

它不是别人的 CLI，而是**一个 HTTP 请求**：

- `agent_backends/api_client.py`：最小客户端，OpenAI 兼容（DeepSeek / OpenAI / 本地 vLLM、Ollama、
  LM Studio）与 Anthropic 兼容两家；SSE 逐行解析，脏行（空行、`:` 注释、`event:`、`[DONE]`）
  一律跳过 —— "解析器不能比上游更脆"这条规矩在 CLI 那边已经立过一次。
- `agent_backends/builtin_agent.py`：实现**与 opencode / CLI 完全相同的端口**
  （`start/resume/generate/chat/abort/dispose`），所以编排层、界面、权限模型一行都没改。
  - 生成走**同一套文本契约**（`===TU-SCRIPT===` …），引擎那侧的解析与回灌自修原样复用；
  - 历史存在运行目录（`chat.jsonl`，与界面回填同一份），`resume()` 读回来 ——
    "重启应用接着上次聊"不依赖任何外部服务；
  - `abort()` / `dispose()` 置位取消事件，正在读的流在下一个数据块处停下。
- **安全模型更窄了**：它没有任何工具（不执行命令、不读写文件、不联网，除了调用模型 API），
  产物只有文本；脚本仍由引擎解析、写盘、shellcheck、人工确认后才执行。
- **思考过程看得见**：`reasoning_content`（DeepSeek-R1 类）与 `thinking_delta`（Anthropic）
  都逐字回调，对话记录里自动分成两段：

```
—— 思考过程 ——
（模型的推理，逐字出现）
—— 回复 ——
（最终答案）
```

- **模型列表是真的**：命令行后端只能给候选常量（`sonnet/opus/haiku`），内置后端可以
  `GET {base}/models` 现取（「检测可用模型」与对话面板的下拉共用）——用户抱怨的"检测不了模型"
  在这一路上不再存在。
- **「检测」也是真的**：不是检查"字段填没填"，而是真发一次列模型请求；key 错了就报
  "HTTP 401 …检查「设置 → 内置 agent」里的 API key"。

设置页新增三行：API 地址 / API key（按密码显示）/ 接口风格（OpenAI 兼容、Anthropic 兼容），
只在后端选到内置时可编辑，其余后端置灰；命令框在内置后端下也置灰（它没有命令）。

### 用例（`tests/test_builtin_agent.py`，17 条）

地址拼接容错、流式把思考与正文分开、切段时插标题、Anthropic 风格、401/连接错误的中文可行动提示、
取消让流停下、真实模型列表、生成过契约、契约缺失时给可重试的错误、对话流式 + 历史带进下一轮、
没填模型时的提示、`resume` 读回历史、`abort` 中断在飞的一轮、注册表接线（造适配器 / 检测 / 列模型）、
字段没填不发请求。
**外加一条端到端**：`test_our_own_agent_drives_the_whole_loop` —— 假模型 API（两轮 SSE：先给会被
shellcheck 拦下的脚本、再给修好的）+ **真实** `run_loop`、真实 shellcheck、真实 bash、真实运行目录：
两轮各发一次 HTTP（不起进程）、第一轮被 SC2045 拦下、第二轮执行成功、第二轮提示里带着 shellcheck 反馈。


## 修订三十（2026-09-20）：用户填 DeepSeek 的 Anthropic 兼容端点，卡在"正在获取模型"

用户报："我的 API 地址是 https://api.deepseek.com/anthropic，选择什么接口风格，
为什么他一直显示：正在向模型 API 获取可用模型"。

### 答案与两个真 bug

**该选「Anthropic 兼容」**（地址里的 `/anthropic` 就是 DeepSeek 的 Anthropic 兼容入口，
对话地址是 `{base}/v1/messages`）。但用户卡住的原因不止"选错了风格"，还有两个我的实现缺陷：

1. **列模型的超时用了对话那一档（300 秒）**。`ModelApiClient` 只有 `DEFAULT_TIMEOUT_S = 300`，
   而"列模型/检测"只是问一句"能不能用" —— 地址不通时界面就一直停在"正在获取…"上五分钟
   （用户看到的就是这个）。现在单列一档 `LIST_TIMEOUT_S = 20`，并断言请求上真的带了它
   （`httpx` 把超时放进 `request.extensions`）。
2. **Anthropic 兼容端点没有 `/v1/models`**。DeepSeek 的 `/anthropic` 只实现对话那条路。
   现在 `models_url_candidates()` 会依次试：风格自己的地址 → **同一站点的 OpenAI 兼容根**
   （`https://api.deepseek.com/models`、`/v1/models`），404/405 就换下一个；
   401/403 这种"配置不对"立刻报出来（换地址没用）；全失败时把**试过哪些地址**一起报给用户。

顺带修掉一个会在国内机器上普遍发生的坑：**环境里配了 SOCKS 代理**（`ALL_PROXY=socks5://…`）
而 `httpx` 缺 `socksio` 时，建客户端就 `ImportError`，用户看到的是
`Using SOCKS proxy, but the 'socksio' package is not installed`。现在：
把依赖声明改成 `httpx[socks]>=0.27`（并已安装），构造失败时也翻译成一句可行动的话
（"装 `httpx[socks]`，或临时清掉 ALL_PROXY / HTTPS_PROXY"）。

### 界面上主动指路（不指望用户记住规则）

`builtin.suggested_style(base)` 按地址猜风格（含 `/anthropic` → Anthropic；含 `/v1`、`openai`、
本地端口 → OpenAI），设置页在**地址与风格不匹配时报出来**：
`⚠ 这个地址看着是「Anthropic 兼容」端点：接口风格请改成「Anthropic 兼容」。…`

### 用例（`tests/test_builtin_agent.py` 增到 24 条）

DeepSeek 那个地址 → 建议 Anthropic 风格、对话地址拼成 `…/anthropic/v1/messages`、
列模型按候选地址回退（第一次 404、第二次 200，断言真的问了两个地址）、
全失败时报出试过的地址、列模型带短超时、SOCKS 缺 socksio 的可行动提示、
设置页在地址与风格不匹配时给 ⚠。变异验证四条全红。


## 修订三十一（2026-09-20）：内置 agent 与 opencode 的能力对齐 + 技能（skill）

用户的要求（原话）："你做的这个 agent 有什么好处吗，可不可以增加 skill 什么的，
与 opencode 相比更加聪明吗还是什么，我这边推荐你必须最少跟 opencode 的功能保持一致"。

### 先如实回答"好在哪、是不是更聪明"

**不是更聪明**。同样的模型，谁调都是那个模型；差别只在"通道"：

| | opencode 那条路 | 内置 agent |
| --- | --- | --- |
| 谁来生成 | `opencode serve` 的 HTTP API | 直接 POST 模型 API |
| 首字延迟 | 首次要起 serve + 建会话 | 只有模型自己的首 token |
| 思考过程 | 由 CLI 决定，界面拿不到 | `reasoning_content` / `thinking_delta` **逐字显示** |
| 工具能力 | agent 定义文件里逐键 deny | **没有工具**（比它更窄：不碰盘、不跑命令） |
| 依赖 | 必须装 opencode + 登录 | 一个 API key |
| 模型列表 | `opencode models` | `GET {base}/models`（真列表） |

内置 agent 的**好处**是：快（不起进程）、透明（看得到推理）、依赖少（不用装东西）、
边界更窄（没有任何工具）。它的**代价**是没有 opencode 的生态（插件、agent 定义、
多模型路由、会话服务端管理）。

### 对齐清单（本轮补上的四件事）

前一轮的内置 agent 只是"能说话"，与 opencode 比缺了四件**工程上必须有**的东西，
本轮补齐，每条都有用例与变异验证：

| # | 缺什么 | 补法 |
| --- | --- | --- |
| 1 | **忽略引擎给的超时**（写死客户端 300 秒默认值） | 每次调用把 `timeout_ms` 变成请求超时（与命令行后端一致） |
| 2 | **瞬时故障不重试**（opencode/CLI 有进程级韧性） | 未吐字时对 连不上/读超时/408/409/425/429/5xx 退避重试（默认 2 次）；401/403/404 **不重试**（那是配置错，重试只让用户多等） |
| 3 | **不管上下文窗口**（每轮把全部历史发出去） | 按字符预算裁剪历史（默认 300k 字符），从最旧的开始丢、**永远保留本轮提问**，并在最前面留一句"更早的对话已省略" |
| 4 | **没有技能机制** | 新增 `agent_backends/skills.py`（见下） |

### 技能（skill）

技能就是**纯提示词资产**：一个目录，每个技能一份 `SKILL.md`（front matter 可选），
正文拼进系统提示。**不是代码、不执行任何东西** —— 与方案文档走同一条路进提示词。

```
skills/
  shell-strict/SKILL.md     ← 仓库自带（随代码版本化、可评审）
```

- 解析**宽容**：没有 front matter、没有 description、直接放 `<名字>.md` 都能认；
  `README.md` / 下划线开头的文件跳过（自己在验证时立刻撞到这个：README 被当成技能注入了）；
  空文件与读不出来的文件**报出来**（"哪个技能没被加载"用户要能看到），不静默消失。
- 设置页新增「技能目录」「启用技能」两行：留空 = 用默认目录/全部技能；提示行实时显示
  "目录：… 发现：… 本次注入：… 未加载：…"。
- **打包产物里没有 `skills/` 目录**（PyInstaller 只收代码），所以随应用交付的内置技能
  （`BUILTIN_SKILLS`）会在默认目录不存在时**按需落盘** —— 与内置模板同一套做法；
  已有文件不覆盖（用户改过的内容不该被还原），并且有用例比对"仓库里那份文件与内置常量一致"。
- 自带一个真有用的技能：`shell-strict`（引号、`set -euo pipefail`、危险命令保护、可重复执行）——
  它同时是"生成质量"的一部分：这些正是评审与 shellcheck 会盯的地方。
- 只对**内置 agent** 生效（opencode / claude / codeagent 有各自的技能机制，那不属于我们）。

### 用例

`tests/test_builtin_agent.py` 24 → 33 条：每次调用带引擎给的超时、瞬时错误重试而 401 不重试、
长历史按预算裁剪且保留本轮提问、技能发现与注入系统提示、只注入启用的技能、
仓库自带技能可解析、内置技能按需落盘且不覆盖用户改动、
仓库文件与内置常量一致、控制器在默认目录缺失时落盘。
变异验证五条全红：忽略超时 / 不重试 / 不裁剪 / 不注入技能 / 把 README 当技能。
